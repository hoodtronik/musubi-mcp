============================================================
### cache_latents.py
============================================================
W0416 15:03:44.148000 43780 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: cache_latents.py [-h] --dataset_config DATASET_CONFIG [--vae VAE]
                        [--vae_dtype VAE_DTYPE] [--device DEVICE]
                        [--batch_size BATCH_SIZE] [--num_workers NUM_WORKERS]
                        [--skip_existing] [--keep_cache]
                        [--debug_mode {image,console,video}]
                        [--console_width CONSOLE_WIDTH]
                        [--console_back CONSOLE_BACK]
                        [--console_num_images CONSOLE_NUM_IMAGES]
                        [--disable_cudnn_backend] [--vae_tiling]
                        [--vae_chunk_size VAE_CHUNK_SIZE]
                        [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256

============================================================
### cache_text_encoder_outputs.py
============================================================
usage: cache_text_encoder_outputs.py [-h] --dataset_config DATASET_CONFIG
                                     [--device DEVICE]
                                     [--batch_size BATCH_SIZE]
                                     [--num_workers NUM_WORKERS]
                                     [--skip_existing] [--keep_cache]
                                     --text_encoder1 TEXT_ENCODER1
                                     --text_encoder2 TEXT_ENCODER2
                                     [--text_encoder_dtype TEXT_ENCODER_DTYPE]
                                     [--fp8_llm]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory
  --text_encoder_dtype TEXT_ENCODER_DTYPE
                        data type for Text Encoder, default is float16
  --fp8_llm             use fp8 for Text Encoder 1 (LLM)

============================================================
### wan_cache_latents.py
============================================================
W0416 15:03:53.680000 61172 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: wan_cache_latents.py [-h] --dataset_config DATASET_CONFIG [--vae VAE]
                            [--vae_dtype VAE_DTYPE] [--device DEVICE]
                            [--batch_size BATCH_SIZE]
                            [--num_workers NUM_WORKERS] [--skip_existing]
                            [--keep_cache]
                            [--debug_mode {image,console,video}]
                            [--console_width CONSOLE_WIDTH]
                            [--console_back CONSOLE_BACK]
                            [--console_num_images CONSOLE_NUM_IMAGES]
                            [--disable_cudnn_backend] [--vae_cache_cpu]
                            [--i2v] [--clip CLIP] [--one_frame]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --vae_cache_cpu       cache features in VAE on CPU
  --i2v                 I2V model, encode the first frame as control image. If
                        clip is set, this is automatically set to True
  --clip CLIP           text encoder (CLIP) checkpoint path, optional. If
                        training Wan2.1 I2V model, this is required
  --one_frame           Generate cache for one frame training (single frame,
                        single section).

============================================================
### wan_cache_text_encoder_outputs.py
============================================================
usage: wan_cache_text_encoder_outputs.py [-h] --dataset_config DATASET_CONFIG
                                         [--device DEVICE]
                                         [--batch_size BATCH_SIZE]
                                         [--num_workers NUM_WORKERS]
                                         [--skip_existing] [--keep_cache] --t5
                                         T5 [--fp8_t5]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --t5 T5               text encoder (T5) checkpoint path
  --fp8_t5              use fp8 for Text Encoder model

============================================================
### wan_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: wan_train_network.py [-h] [--config_file CONFIG_FILE]
                            [--dataset_config DATASET_CONFIG] [--sdpa]
                            [--flash_attn] [--sage_attn] [--xformers]
                            [--flash3] [--split_attn] [--compile]
                            [--compile_backend COMPILE_BACKEND]
                            [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                            [--compile_dynamic {true,false,auto}]
                            [--compile_fullgraph]
                            [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                            [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                            [--max_train_steps MAX_TRAIN_STEPS]
                            [--max_train_epochs MAX_TRAIN_EPOCHS]
                            [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                            [--persistent_data_loader_workers] [--seed SEED]
                            [--gradient_checkpointing]
                            [--gradient_checkpointing_cpu_offload]
                            [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                            [--mixed_precision {no,fp16,bf16}]
                            [--logging_dir LOGGING_DIR]
                            [--log_with {tensorboard,wandb,all}]
                            [--log_prefix LOG_PREFIX]
                            [--log_tracker_name LOG_TRACKER_NAME]
                            [--wandb_run_name WANDB_RUN_NAME]
                            [--log_tracker_config LOG_TRACKER_CONFIG]
                            [--wandb_api_key WANDB_API_KEY] [--log_config]
                            [--ddp_timeout DDP_TIMEOUT]
                            [--ddp_gradient_as_bucket_view]
                            [--ddp_static_graph]
                            [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                            [--sample_at_first]
                            [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                            [--sample_prompts SAMPLE_PROMPTS]
                            [--optimizer_type OPTIMIZER_TYPE]
                            [--optimizer_args [OPTIMIZER_ARGS ...]]
                            [--learning_rate LEARNING_RATE]
                            [--max_grad_norm MAX_GRAD_NORM]
                            [--lr_scheduler LR_SCHEDULER]
                            [--lr_warmup_steps LR_WARMUP_STEPS]
                            [--lr_decay_steps LR_DECAY_STEPS]
                            [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                            [--lr_scheduler_power LR_SCHEDULER_POWER]
                            [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                            [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                            [--lr_scheduler_type LR_SCHEDULER_TYPE]
                            [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                            [--fp8_base]
                            [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                            [--dynamo_mode {default,reduce-overhead,max-autotune}]
                            [--dynamo_fullgraph] [--dynamo_dynamic]
                            [--blocks_to_swap BLOCKS_TO_SWAP]
                            [--use_pinned_memory_for_block_swap]
                            [--img_in_txt_in_offloading]
                            [--disable_numpy_memmap]
                            [--guidance_scale GUIDANCE_SCALE]
                            [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                            [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                            [--sigmoid_scale SIGMOID_SCALE]
                            [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                            [--logit_mean LOGIT_MEAN] [--logit_std LOGIT_STD]
                            [--mode_scale MODE_SCALE]
                            [--min_timestep MIN_TIMESTEP]
                            [--max_timestep MAX_TIMESTEP]
                            [--preserve_distribution_shape]
                            [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                            [--show_timesteps {image,console}] [--no_metadata]
                            [--network_weights NETWORK_WEIGHTS]
                            [--network_module NETWORK_MODULE]
                            [--network_dim NETWORK_DIM]
                            [--network_alpha NETWORK_ALPHA]
                            [--network_dropout NETWORK_DROPOUT]
                            [--network_args [NETWORK_ARGS ...]]
                            [--training_comment TRAINING_COMMENT]
                            [--dim_from_weights]
                            [--scale_weight_norms SCALE_WEIGHT_NORMS]
                            [--base_weights [BASE_WEIGHTS ...]]
                            [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                            [--output_dir OUTPUT_DIR]
                            [--output_name OUTPUT_NAME] [--resume RESUME]
                            [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                            [--save_every_n_steps SAVE_EVERY_N_STEPS]
                            [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                            [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                            [--save_last_n_steps SAVE_LAST_N_STEPS]
                            [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                            [--save_state] [--save_state_on_train_end]
                            [--metadata_title METADATA_TITLE]
                            [--metadata_author METADATA_AUTHOR]
                            [--metadata_description METADATA_DESCRIPTION]
                            [--metadata_license METADATA_LICENSE]
                            [--metadata_tags METADATA_TAGS]
                            [--metadata_reso METADATA_RESO]
                            [--metadata_arch METADATA_ARCH]
                            [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                            [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                            [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                            [--huggingface_token HUGGINGFACE_TOKEN]
                            [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                            [--save_state_to_huggingface]
                            [--resume_from_huggingface] [--async_upload]
                            [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                            [--task {t2v-14B,t2v-1.3B,i2v-14B,t2i-14B,flf2v-14B,t2v-1.3B-FC,t2v-14B-FC,i2v-14B-FC,i2v-A14B,t2v-A14B}]
                            [--fp8_scaled] [--t5 T5] [--fp8_t5] [--clip CLIP]
                            [--vae_cache_cpu] [--one_frame]
                            [--force_v2_1_time_embedding]
                            [--dit_high_noise DIT_HIGH_NOISE]
                            [--timestep_boundary TIMESTEP_BOUNDARY]
                            [--offload_inactive_dit]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --task {t2v-14B,t2v-1.3B,i2v-14B,t2i-14B,flf2v-14B,t2v-1.3B-FC,t2v-14B-FC,i2v-14B-FC,i2v-A14B,t2v-A14B}
                        The task to run.
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --t5 T5               text encoder (T5) checkpoint path
  --fp8_t5              use fp8 for Text Encoder model
  --clip CLIP           text encoder (CLIP) checkpoint path, optional. If
                        training Wan2.1 I2V model, this is required
  --vae_cache_cpu       cache features in VAE on CPU
  --one_frame           Use one frame sampling method for sample generation
  --force_v2_1_time_embedding
                        Force using 2.1 style time embedding even for Wan 2.2
  --dit_high_noise DIT_HIGH_NOISE
                        DiT checkpoint path for high noise model
  --timestep_boundary TIMESTEP_BOUNDARY
                        Timestep boundary for switching between high and low
                        noise models, defaults to None (task specific) /
                        高ノイズモデルと低ノイズモデルを切り替えるタイムステップ境界。デフォルトはNone（タスク固有）
  --offload_inactive_dit
                        Offload inactive DiT model to CPU. Cannot be used with
                        block swap /
                        アクティブではないDiTモデルをCPUにオフロードします。ブロックスワップと併用できません

============================================================
### wan_generate_video.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: wan_generate_video.py [-h] [--ckpt_dir CKPT_DIR]
                             [--task {t2v-14B,t2v-1.3B,i2v-14B,t2i-14B,flf2v-14B,t2v-1.3B-FC,t2v-14B-FC,i2v-14B-FC,i2v-A14B,t2v-A14B}]
                             [--sample_solver {unipc,dpm++,vanilla}]
                             [--dit DIT] [--dit_high_noise DIT_HIGH_NOISE]
                             [--force_v2_1_time_embedding]
                             [--offload_inactive_dit] [--lazy_loading]
                             [--disable_numpy_memmap] [--vae VAE]
                             [--vae_dtype VAE_DTYPE] [--vae_cache_cpu]
                             [--t5 T5] [--clip CLIP]
                             [--lora_weight [LORA_WEIGHT ...]]
                             [--lora_multiplier [LORA_MULTIPLIER ...]]
                             [--lora_weight_high_noise [LORA_WEIGHT_HIGH_NOISE ...]]
                             [--lora_multiplier_high_noise [LORA_MULTIPLIER_HIGH_NOISE ...]]
                             [--include_patterns [INCLUDE_PATTERNS ...]]
                             [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                             [--save_merged_model SAVE_MERGED_MODEL]
                             [--prompt PROMPT]
                             [--negative_prompt NEGATIVE_PROMPT]
                             [--video_size VIDEO_SIZE VIDEO_SIZE]
                             [--video_length VIDEO_LENGTH] [--fps FPS]
                             [--infer_steps INFER_STEPS] --save_path SAVE_PATH
                             [--seed SEED] [--cpu_noise]
                             [--timestep_boundary TIMESTEP_BOUNDARY]
                             [--guidance_scale GUIDANCE_SCALE]
                             [--guidance_scale_high_noise GUIDANCE_SCALE_HIGH_NOISE]
                             [--video_path VIDEO_PATH]
                             [--image_path IMAGE_PATH]
                             [--end_image_path END_IMAGE_PATH]
                             [--control_path CONTROL_PATH]
                             [--one_frame_inference ONE_FRAME_INFERENCE]
                             [--control_image_path [CONTROL_IMAGE_PATH ...]]
                             [--control_image_mask_path [CONTROL_IMAGE_MASK_PATH ...]]
                             [--trim_tail_frames TRIM_TAIL_FRAMES]
                             [--cfg_skip_mode {early,late,middle,early_late,alternate,none}]
                             [--cfg_apply_ratio CFG_APPLY_RATIO]
                             [--slg_layers SLG_LAYERS] [--slg_scale SLG_SCALE]
                             [--slg_start SLG_START] [--slg_end SLG_END]
                             [--slg_mode {original,uncond}]
                             [--flow_shift FLOW_SHIFT] [--fp8] [--fp8_scaled]
                             [--fp8_fast] [--fp8_t5] [--device DEVICE]
                             [--attn_mode {flash,flash2,flash3,torch,sageattn,xformers,sdpa}]
                             [--blocks_to_swap BLOCKS_TO_SWAP]
                             [--use_pinned_memory_for_block_swap]
                             [--output_type {video,images,latent,both,latent_images}]
                             [--no_metadata] [--latent_path [LATENT_PATH ...]]
                             [--lycoris]
                             [--compile_args BACKEND MODE DYNAMIC FULLGRAPH]
                             [--compile] [--compile_backend COMPILE_BACKEND]
                             [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                             [--compile_dynamic {true,false,auto}]
                             [--compile_fullgraph]
                             [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                             [--from_file FROM_FILE] [--interactive]

Wan 2.1 inference script

options:
  -h, --help            show this help message and exit
  --ckpt_dir CKPT_DIR   The path to the checkpoint directory (Wan 2.1
                        official).
  --task {t2v-14B,t2v-1.3B,i2v-14B,t2i-14B,flf2v-14B,t2v-1.3B-FC,t2v-14B-FC,i2v-14B-FC,i2v-A14B,t2v-A14B}
                        The task to run.
  --sample_solver {unipc,dpm++,vanilla}
                        The solver used to sample.
  --dit DIT             DiT checkpoint path
  --dit_high_noise DIT_HIGH_NOISE
                        DiT checkpoint path for high noise (optional)
  --force_v2_1_time_embedding
                        Force using 2.1 style time embedding even for Wan 2.2
  --offload_inactive_dit
                        Offload DiT model to CPU
  --lazy_loading        Enable lazy loading for DiT models
  --disable_numpy_memmap
                        Disable numpy memmap when loading safetensors. Default
                        is False.
  --vae VAE             VAE checkpoint path
  --vae_dtype VAE_DTYPE
                        data type for VAE, default is bfloat16
  --vae_cache_cpu       cache features in VAE on CPU
  --t5 T5               text encoder (T5) checkpoint path
  --clip CLIP           text encoder (CLIP) checkpoint path
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --lora_weight_high_noise [LORA_WEIGHT_HIGH_NOISE ...]
                        LoRA weight path for high noise
  --lora_multiplier_high_noise [LORA_MULTIPLIER_HIGH_NOISE ...]
                        LoRA multiplier for high noise
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --prompt PROMPT       prompt for generation
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation, use default negative
                        prompt if not specified
  --video_size VIDEO_SIZE VIDEO_SIZE
                        video size, height and width
  --video_length VIDEO_LENGTH
                        video length, Default depends on task
  --fps FPS             video fps, Default is 16
  --infer_steps INFER_STEPS
                        number of inference steps, default depends on task
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --cpu_noise           Use CPU to generate noise (compatible with ComfyUI).
                        Default is False.
  --timestep_boundary TIMESTEP_BOUNDARY
                        Timestep boundary for guidance (0.0 to 1.0). Default
                        depends on task.
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        depends on task.
  --guidance_scale_high_noise GUIDANCE_SCALE_HIGH_NOISE
                        Guidance scale for classifier free guidance in high
                        noise model. Default depends on task.
  --video_path VIDEO_PATH
                        path to video for video2video inference
  --image_path IMAGE_PATH
                        path to image for image2video inference
  --end_image_path END_IMAGE_PATH
                        path to end image for image2video inference
  --control_path CONTROL_PATH
                        path to control video for inference with controlnet.
                        video file or directory with images
  --one_frame_inference ONE_FRAME_INFERENCE
                        one frame inference, default is None, comma separated
                        values from 'no_2x', 'no_4x', 'no_post',
                        'control_indices' and 'target_index'.
  --control_image_path [CONTROL_IMAGE_PATH ...]
                        path to control (reference) image for one frame
                        inference.
  --control_image_mask_path [CONTROL_IMAGE_MASK_PATH ...]
                        path to control (reference) image mask for one frame
                        inference.
  --trim_tail_frames TRIM_TAIL_FRAMES
                        trim tail N frames from the video before saving
  --cfg_skip_mode {early,late,middle,early_late,alternate,none}
                        CFG skip mode. each mode skips different parts of the
                        CFG. early: initial steps, late: later steps, middle:
                        middle steps, early_late: both early and late,
                        alternate: alternate, none: no skip (default)
  --cfg_apply_ratio CFG_APPLY_RATIO
                        The ratio of steps to apply CFG (0.0 to 1.0). Default
                        is None (apply all steps).
  --slg_layers SLG_LAYERS
                        Skip block (layer) indices for SLG (Skip Layer
                        Guidance), comma separated
  --slg_scale SLG_SCALE
                        scale for SLG classifier free guidance. Default is
                        3.0. Ignored if slg_mode is None or uncond
  --slg_start SLG_START
                        start ratio for inference steps for SLG. Default is
                        0.0.
  --slg_end SLG_END     end ratio for inference steps for SLG. Default is 0.3.
  --slg_mode {original,uncond}
                        SLG mode. original: same as SD3, uncond: replace
                        uncond pred with SLG pred
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default
                        depends on task.
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --fp8_fast            Enable fast FP8 arithmetic (RTX 4XXX+), only for
                        fp8_scaled
  --fp8_t5              use fp8 for Text Encoder model
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,flash2,flash3,torch,sageattn,xformers,sdpa}
                        attention mode
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {video,images,latent,both,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --compile_args BACKEND MODE DYNAMIC FULLGRAPH
                        [Deprecated] Torch.compile settings. Use individual
                        arguments instead.
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console

============================================================
### flux_2_cache_latents.py
============================================================
W0416 15:04:12.175000 61008 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: flux_2_cache_latents.py [-h] --dataset_config DATASET_CONFIG
                               [--vae VAE] [--vae_dtype VAE_DTYPE]
                               [--device DEVICE] [--batch_size BATCH_SIZE]
                               [--num_workers NUM_WORKERS] [--skip_existing]
                               [--keep_cache]
                               [--debug_mode {image,console,video}]
                               [--console_width CONSOLE_WIDTH]
                               [--console_back CONSOLE_BACK]
                               [--console_num_images CONSOLE_NUM_IMAGES]
                               [--disable_cudnn_backend]
                               [--model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}
                        model version

============================================================
### flux_2_cache_text_encoder_outputs.py
============================================================
usage: flux_2_cache_text_encoder_outputs.py [-h] --dataset_config
                                            DATASET_CONFIG [--device DEVICE]
                                            [--batch_size BATCH_SIZE]
                                            [--num_workers NUM_WORKERS]
                                            [--skip_existing] [--keep_cache]
                                            --text_encoder TEXT_ENCODER
                                            [--fp8_text_encoder]
                                            [--model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder TEXT_ENCODER
                        text encoder (mistral 3) checkpoint path
  --fp8_text_encoder    use fp8 for Text Encoder model
  --model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}
                        model version

============================================================
### flux_2_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: flux_2_train_network.py [-h] [--config_file CONFIG_FILE]
                               [--dataset_config DATASET_CONFIG] [--sdpa]
                               [--flash_attn] [--sage_attn] [--xformers]
                               [--flash3] [--split_attn] [--compile]
                               [--compile_backend COMPILE_BACKEND]
                               [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                               [--compile_dynamic {true,false,auto}]
                               [--compile_fullgraph]
                               [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                               [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                               [--max_train_steps MAX_TRAIN_STEPS]
                               [--max_train_epochs MAX_TRAIN_EPOCHS]
                               [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                               [--persistent_data_loader_workers]
                               [--seed SEED] [--gradient_checkpointing]
                               [--gradient_checkpointing_cpu_offload]
                               [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                               [--mixed_precision {no,fp16,bf16}]
                               [--logging_dir LOGGING_DIR]
                               [--log_with {tensorboard,wandb,all}]
                               [--log_prefix LOG_PREFIX]
                               [--log_tracker_name LOG_TRACKER_NAME]
                               [--wandb_run_name WANDB_RUN_NAME]
                               [--log_tracker_config LOG_TRACKER_CONFIG]
                               [--wandb_api_key WANDB_API_KEY] [--log_config]
                               [--ddp_timeout DDP_TIMEOUT]
                               [--ddp_gradient_as_bucket_view]
                               [--ddp_static_graph]
                               [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                               [--sample_at_first]
                               [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                               [--sample_prompts SAMPLE_PROMPTS]
                               [--optimizer_type OPTIMIZER_TYPE]
                               [--optimizer_args [OPTIMIZER_ARGS ...]]
                               [--learning_rate LEARNING_RATE]
                               [--max_grad_norm MAX_GRAD_NORM]
                               [--lr_scheduler LR_SCHEDULER]
                               [--lr_warmup_steps LR_WARMUP_STEPS]
                               [--lr_decay_steps LR_DECAY_STEPS]
                               [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                               [--lr_scheduler_power LR_SCHEDULER_POWER]
                               [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                               [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                               [--lr_scheduler_type LR_SCHEDULER_TYPE]
                               [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                               [--fp8_base]
                               [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                               [--dynamo_mode {default,reduce-overhead,max-autotune}]
                               [--dynamo_fullgraph] [--dynamo_dynamic]
                               [--blocks_to_swap BLOCKS_TO_SWAP]
                               [--use_pinned_memory_for_block_swap]
                               [--img_in_txt_in_offloading]
                               [--disable_numpy_memmap]
                               [--guidance_scale GUIDANCE_SCALE]
                               [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                               [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                               [--sigmoid_scale SIGMOID_SCALE]
                               [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                               [--logit_mean LOGIT_MEAN]
                               [--logit_std LOGIT_STD]
                               [--mode_scale MODE_SCALE]
                               [--min_timestep MIN_TIMESTEP]
                               [--max_timestep MAX_TIMESTEP]
                               [--preserve_distribution_shape]
                               [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                               [--show_timesteps {image,console}]
                               [--no_metadata]
                               [--network_weights NETWORK_WEIGHTS]
                               [--network_module NETWORK_MODULE]
                               [--network_dim NETWORK_DIM]
                               [--network_alpha NETWORK_ALPHA]
                               [--network_dropout NETWORK_DROPOUT]
                               [--network_args [NETWORK_ARGS ...]]
                               [--training_comment TRAINING_COMMENT]
                               [--dim_from_weights]
                               [--scale_weight_norms SCALE_WEIGHT_NORMS]
                               [--base_weights [BASE_WEIGHTS ...]]
                               [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                               [--output_dir OUTPUT_DIR]
                               [--output_name OUTPUT_NAME] [--resume RESUME]
                               [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                               [--save_every_n_steps SAVE_EVERY_N_STEPS]
                               [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                               [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                               [--save_last_n_steps SAVE_LAST_N_STEPS]
                               [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                               [--save_state] [--save_state_on_train_end]
                               [--metadata_title METADATA_TITLE]
                               [--metadata_author METADATA_AUTHOR]
                               [--metadata_description METADATA_DESCRIPTION]
                               [--metadata_license METADATA_LICENSE]
                               [--metadata_tags METADATA_TAGS]
                               [--metadata_reso METADATA_RESO]
                               [--metadata_arch METADATA_ARCH]
                               [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                               [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                               [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                               [--huggingface_token HUGGINGFACE_TOKEN]
                               [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                               [--save_state_to_huggingface]
                               [--resume_from_huggingface] [--async_upload]
                               [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                               [--fp8_scaled] [--text_encoder TEXT_ENCODER]
                               [--fp8_text_encoder]
                               [--model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --text_encoder TEXT_ENCODER
                        text encoder checkpoint path
  --fp8_text_encoder    use fp8 for Text Encoder model
  --model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}
                        model version

============================================================
### flux_2_generate_image.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: flux_2_generate_image.py [-h] [--dit DIT] [--disable_numpy_memmap]
                                [--vae VAE] --text_encoder TEXT_ENCODER
                                [--lora_weight [LORA_WEIGHT ...]]
                                [--lora_multiplier [LORA_MULTIPLIER ...]]
                                [--include_patterns [INCLUDE_PATTERNS ...]]
                                [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                                [--save_merged_model SAVE_MERGED_MODEL]
                                [--guidance_scale GUIDANCE_SCALE]
                                [--prompt PROMPT]
                                [--negative_prompt NEGATIVE_PROMPT]
                                [--image_size IMAGE_SIZE IMAGE_SIZE]
                                [--control_image_path [CONTROL_IMAGE_PATH ...]]
                                [--no_resize_control]
                                [--infer_steps INFER_STEPS] --save_path
                                SAVE_PATH [--seed SEED]
                                [--embedded_cfg_scale EMBEDDED_CFG_SCALE]
                                [--flow_shift FLOW_SHIFT] [--fp8]
                                [--fp8_scaled] [--fp8_text_encoder]
                                [--device DEVICE]
                                [--attn_mode {flash,torch,sageattn,xformers,sdpa}]
                                [--blocks_to_swap BLOCKS_TO_SWAP]
                                [--use_pinned_memory_for_block_swap]
                                [--output_type {images,latent,latent_images}]
                                [--no_metadata]
                                [--latent_path [LATENT_PATH ...]] [--lycoris]
                                [--compile]
                                [--compile_backend COMPILE_BACKEND]
                                [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                [--compile_dynamic {true,false,auto}]
                                [--compile_fullgraph]
                                [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                [--from_file FROM_FILE] [--interactive]
                                [--model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}]

FLUX.2 inference script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT directory or path
  --disable_numpy_memmap
                        Disable numpy memmap when loading safetensors. Default
                        is False.
  --vae VAE             AE directory or path
  --text_encoder TEXT_ENCODER
                        Text Encoder Mistral 3/Qwen 3 directory or path
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        is 4.0.
  --prompt PROMPT       prompt for generation
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation, default is None (` `
                        for non-distilled model)
  --image_size IMAGE_SIZE IMAGE_SIZE
                        image size, height and width
  --control_image_path [CONTROL_IMAGE_PATH ...]
                        path to control (reference) image(s) for Flux 2 image
                        edit
  --no_resize_control   Do not resize control image (default is to resize if
                        too large)
  --infer_steps INFER_STEPS
                        number of inference steps, default is 25
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --embedded_cfg_scale EMBEDDED_CFG_SCALE
                        Embeded CFG scale (distilled CFG Scale), default is
                        4.0. All klein models ignore this.
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default is
                        None (FLUX.2 default).
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --fp8_text_encoder    use fp8 for Text Encoder (Mistral 3)
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,torch,sageattn,xformers,sdpa}
                        attention mode
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {images,latent,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console
  --model_version {klein-4b,klein-base-4b,klein-9b,klein-base-9b,dev}
                        model version

============================================================
### zimage_cache_latents.py
============================================================
W0416 15:04:29.355000 56664 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: zimage_cache_latents.py [-h] --dataset_config DATASET_CONFIG
                               [--vae VAE] [--vae_dtype VAE_DTYPE]
                               [--device DEVICE] [--batch_size BATCH_SIZE]
                               [--num_workers NUM_WORKERS] [--skip_existing]
                               [--keep_cache]
                               [--debug_mode {image,console,video}]
                               [--console_width CONSOLE_WIDTH]
                               [--console_back CONSOLE_BACK]
                               [--console_num_images CONSOLE_NUM_IMAGES]
                               [--disable_cudnn_backend]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.

============================================================
### zimage_cache_text_encoder_outputs.py
============================================================
usage: zimage_cache_text_encoder_outputs.py [-h] --dataset_config
                                            DATASET_CONFIG [--device DEVICE]
                                            [--batch_size BATCH_SIZE]
                                            [--num_workers NUM_WORKERS]
                                            [--skip_existing] [--keep_cache]
                                            --text_encoder TEXT_ENCODER
                                            [--fp8_llm]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder TEXT_ENCODER
                        Qwen3 text encoder checkpoint path or directory
  --fp8_llm             Use fp8 precision for Qwen3 text encoder (reduces
                        memory usage)

============================================================
### zimage_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: zimage_train_network.py [-h] [--config_file CONFIG_FILE]
                               [--dataset_config DATASET_CONFIG] [--sdpa]
                               [--flash_attn] [--sage_attn] [--xformers]
                               [--flash3] [--split_attn] [--compile]
                               [--compile_backend COMPILE_BACKEND]
                               [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                               [--compile_dynamic {true,false,auto}]
                               [--compile_fullgraph]
                               [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                               [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                               [--max_train_steps MAX_TRAIN_STEPS]
                               [--max_train_epochs MAX_TRAIN_EPOCHS]
                               [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                               [--persistent_data_loader_workers]
                               [--seed SEED] [--gradient_checkpointing]
                               [--gradient_checkpointing_cpu_offload]
                               [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                               [--mixed_precision {no,fp16,bf16}]
                               [--logging_dir LOGGING_DIR]
                               [--log_with {tensorboard,wandb,all}]
                               [--log_prefix LOG_PREFIX]
                               [--log_tracker_name LOG_TRACKER_NAME]
                               [--wandb_run_name WANDB_RUN_NAME]
                               [--log_tracker_config LOG_TRACKER_CONFIG]
                               [--wandb_api_key WANDB_API_KEY] [--log_config]
                               [--ddp_timeout DDP_TIMEOUT]
                               [--ddp_gradient_as_bucket_view]
                               [--ddp_static_graph]
                               [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                               [--sample_at_first]
                               [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                               [--sample_prompts SAMPLE_PROMPTS]
                               [--optimizer_type OPTIMIZER_TYPE]
                               [--optimizer_args [OPTIMIZER_ARGS ...]]
                               [--learning_rate LEARNING_RATE]
                               [--max_grad_norm MAX_GRAD_NORM]
                               [--lr_scheduler LR_SCHEDULER]
                               [--lr_warmup_steps LR_WARMUP_STEPS]
                               [--lr_decay_steps LR_DECAY_STEPS]
                               [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                               [--lr_scheduler_power LR_SCHEDULER_POWER]
                               [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                               [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                               [--lr_scheduler_type LR_SCHEDULER_TYPE]
                               [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                               [--fp8_base]
                               [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                               [--dynamo_mode {default,reduce-overhead,max-autotune}]
                               [--dynamo_fullgraph] [--dynamo_dynamic]
                               [--blocks_to_swap BLOCKS_TO_SWAP]
                               [--use_pinned_memory_for_block_swap]
                               [--img_in_txt_in_offloading]
                               [--disable_numpy_memmap]
                               [--guidance_scale GUIDANCE_SCALE]
                               [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                               [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                               [--sigmoid_scale SIGMOID_SCALE]
                               [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                               [--logit_mean LOGIT_MEAN]
                               [--logit_std LOGIT_STD]
                               [--mode_scale MODE_SCALE]
                               [--min_timestep MIN_TIMESTEP]
                               [--max_timestep MAX_TIMESTEP]
                               [--preserve_distribution_shape]
                               [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                               [--show_timesteps {image,console}]
                               [--no_metadata]
                               [--network_weights NETWORK_WEIGHTS]
                               [--network_module NETWORK_MODULE]
                               [--network_dim NETWORK_DIM]
                               [--network_alpha NETWORK_ALPHA]
                               [--network_dropout NETWORK_DROPOUT]
                               [--network_args [NETWORK_ARGS ...]]
                               [--training_comment TRAINING_COMMENT]
                               [--dim_from_weights]
                               [--scale_weight_norms SCALE_WEIGHT_NORMS]
                               [--base_weights [BASE_WEIGHTS ...]]
                               [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                               [--output_dir OUTPUT_DIR]
                               [--output_name OUTPUT_NAME] [--resume RESUME]
                               [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                               [--save_every_n_steps SAVE_EVERY_N_STEPS]
                               [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                               [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                               [--save_last_n_steps SAVE_LAST_N_STEPS]
                               [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                               [--save_state] [--save_state_on_train_end]
                               [--metadata_title METADATA_TITLE]
                               [--metadata_author METADATA_AUTHOR]
                               [--metadata_description METADATA_DESCRIPTION]
                               [--metadata_license METADATA_LICENSE]
                               [--metadata_tags METADATA_TAGS]
                               [--metadata_reso METADATA_RESO]
                               [--metadata_arch METADATA_ARCH]
                               [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                               [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                               [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                               [--huggingface_token HUGGINGFACE_TOKEN]
                               [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                               [--save_state_to_huggingface]
                               [--resume_from_huggingface] [--async_upload]
                               [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                               [--fp8_scaled] [--text_encoder TEXT_ENCODER]
                               [--fp8_llm] [--use_32bit_attention]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT
  --text_encoder TEXT_ENCODER
                        Qwen3 text encoder checkpoint path
  --fp8_llm             use fp8 for Text Encoder model
  --use_32bit_attention
                        use 32-bit precision for attention computations in DiT
                        model even when using mixed precision (original
                        behavior)

============================================================
### zimage_train.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: zimage_train.py [-h] [--config_file CONFIG_FILE]
                       [--dataset_config DATASET_CONFIG] [--sdpa]
                       [--flash_attn] [--sage_attn] [--xformers] [--flash3]
                       [--split_attn] [--compile]
                       [--compile_backend COMPILE_BACKEND]
                       [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                       [--compile_dynamic {true,false,auto}]
                       [--compile_fullgraph]
                       [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                       [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                       [--max_train_steps MAX_TRAIN_STEPS]
                       [--max_train_epochs MAX_TRAIN_EPOCHS]
                       [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                       [--persistent_data_loader_workers] [--seed SEED]
                       [--gradient_checkpointing]
                       [--gradient_checkpointing_cpu_offload]
                       [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                       [--mixed_precision {no,fp16,bf16}]
                       [--logging_dir LOGGING_DIR]
                       [--log_with {tensorboard,wandb,all}]
                       [--log_prefix LOG_PREFIX]
                       [--log_tracker_name LOG_TRACKER_NAME]
                       [--wandb_run_name WANDB_RUN_NAME]
                       [--log_tracker_config LOG_TRACKER_CONFIG]
                       [--wandb_api_key WANDB_API_KEY] [--log_config]
                       [--ddp_timeout DDP_TIMEOUT]
                       [--ddp_gradient_as_bucket_view] [--ddp_static_graph]
                       [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                       [--sample_at_first]
                       [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                       [--sample_prompts SAMPLE_PROMPTS]
                       [--optimizer_type OPTIMIZER_TYPE]
                       [--optimizer_args [OPTIMIZER_ARGS ...]]
                       [--learning_rate LEARNING_RATE]
                       [--max_grad_norm MAX_GRAD_NORM]
                       [--lr_scheduler LR_SCHEDULER]
                       [--lr_warmup_steps LR_WARMUP_STEPS]
                       [--lr_decay_steps LR_DECAY_STEPS]
                       [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                       [--lr_scheduler_power LR_SCHEDULER_POWER]
                       [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                       [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                       [--lr_scheduler_type LR_SCHEDULER_TYPE]
                       [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                       [--fp8_base]
                       [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                       [--dynamo_mode {default,reduce-overhead,max-autotune}]
                       [--dynamo_fullgraph] [--dynamo_dynamic]
                       [--blocks_to_swap BLOCKS_TO_SWAP]
                       [--use_pinned_memory_for_block_swap]
                       [--img_in_txt_in_offloading] [--disable_numpy_memmap]
                       [--guidance_scale GUIDANCE_SCALE]
                       [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                       [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                       [--sigmoid_scale SIGMOID_SCALE]
                       [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                       [--logit_mean LOGIT_MEAN] [--logit_std LOGIT_STD]
                       [--mode_scale MODE_SCALE] [--min_timestep MIN_TIMESTEP]
                       [--max_timestep MAX_TIMESTEP]
                       [--preserve_distribution_shape]
                       [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                       [--show_timesteps {image,console}] [--no_metadata]
                       [--network_weights NETWORK_WEIGHTS]
                       [--network_module NETWORK_MODULE]
                       [--network_dim NETWORK_DIM]
                       [--network_alpha NETWORK_ALPHA]
                       [--network_dropout NETWORK_DROPOUT]
                       [--network_args [NETWORK_ARGS ...]]
                       [--training_comment TRAINING_COMMENT]
                       [--dim_from_weights]
                       [--scale_weight_norms SCALE_WEIGHT_NORMS]
                       [--base_weights [BASE_WEIGHTS ...]]
                       [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                       [--output_dir OUTPUT_DIR] [--output_name OUTPUT_NAME]
                       [--resume RESUME]
                       [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                       [--save_every_n_steps SAVE_EVERY_N_STEPS]
                       [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                       [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                       [--save_last_n_steps SAVE_LAST_N_STEPS]
                       [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                       [--save_state] [--save_state_on_train_end]
                       [--metadata_title METADATA_TITLE]
                       [--metadata_author METADATA_AUTHOR]
                       [--metadata_description METADATA_DESCRIPTION]
                       [--metadata_license METADATA_LICENSE]
                       [--metadata_tags METADATA_TAGS]
                       [--metadata_reso METADATA_RESO]
                       [--metadata_arch METADATA_ARCH]
                       [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                       [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                       [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                       [--huggingface_token HUGGINGFACE_TOKEN]
                       [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                       [--save_state_to_huggingface]
                       [--resume_from_huggingface] [--async_upload]
                       [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                       [--fp8_scaled] [--text_encoder TEXT_ENCODER]
                       [--fp8_llm] [--use_32bit_attention] [--full_bf16]
                       [--fused_backward_pass] [--mem_eff_save]
                       [--block_swap_optimizer_patch_params]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT
  --text_encoder TEXT_ENCODER
                        Qwen3 text encoder checkpoint path
  --fp8_llm             use fp8 for Text Encoder model
  --use_32bit_attention
                        use 32-bit precision for attention computations in DiT
                        model even when using mixed precision (original
                        behavior)
  --full_bf16           Enable full bfloat16 training for Z-Image
  --fused_backward_pass
                        Use fused backward pass for Adafactor optimizer
  --mem_eff_save        Enable memory efficient saving (saving states requires
                        use normal saving, so it takes same amount of memory
                        even with this option enabled)
  --block_swap_optimizer_patch_params
                        Patch optimizer parameters for block swap when
                        blocks_to_swap > 0. Only works for some optimizers.
                        Not needed when using --fused_backward_pass. / ブロックスワッ
                        プを使用しているときに、optimizer.stepがエラーになるのを回避するためパッチ。一部のoptimi
                        zerでのみ動作します。--fused_backward_passを使用しているときは指定不要です。

============================================================
### zimage_generate_image.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: zimage_generate_image.py [-h] [--dit DIT] [--disable_numpy_memmap]
                                [--vae VAE] --text_encoder TEXT_ENCODER
                                [--lora_weight [LORA_WEIGHT ...]]
                                [--lora_multiplier [LORA_MULTIPLIER ...]]
                                [--include_patterns [INCLUDE_PATTERNS ...]]
                                [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                                [--save_merged_model SAVE_MERGED_MODEL]
                                [--cpu_noise]
                                [--guidance_scale GUIDANCE_SCALE]
                                [--prompt PROMPT]
                                [--negative_prompt NEGATIVE_PROMPT]
                                [--image_size IMAGE_SIZE IMAGE_SIZE]
                                [--infer_steps INFER_STEPS] --save_path
                                SAVE_PATH [--seed SEED]
                                [--embedded_cfg_scale EMBEDDED_CFG_SCALE]
                                [--flow_shift FLOW_SHIFT] [--fp8]
                                [--fp8_scaled] [--fp8_llm]
                                [--text_encoder_cpu] [--device DEVICE]
                                [--attn_mode {flash,torch,sageattn,xformers,sdpa}]
                                [--use_32bit_attention]
                                [--blocks_to_swap BLOCKS_TO_SWAP]
                                [--use_pinned_memory_for_block_swap]
                                [--output_type {images,latent,latent_images}]
                                [--no_metadata]
                                [--latent_path [LATENT_PATH ...]] [--lycoris]
                                [--compile]
                                [--compile_backend COMPILE_BACKEND]
                                [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                [--compile_dynamic {true,false,auto}]
                                [--compile_fullgraph]
                                [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                [--from_file FROM_FILE] [--interactive]
                                [--bell]

Z-Image inference script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT directory or path
  --disable_numpy_memmap
                        Disable numpy memmap when loading safetensors. Default
                        is False.
  --vae VAE             VAE directory or path
  --text_encoder TEXT_ENCODER
                        Text Encoder 1 (Qwen2.5-VL) directory or path
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --cpu_noise           Use CPU to generate noise (compatible with ComfyUI).
                        Default is False.
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        is 0.0 (no guidance).
  --prompt PROMPT       prompt for generation
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation
  --image_size IMAGE_SIZE IMAGE_SIZE
                        image size, height and width
  --infer_steps INFER_STEPS
                        number of inference steps, default is 25
  --save_path SAVE_PATH
                        path to save generated image(s)
  --seed SEED           Seed for evaluation.
  --embedded_cfg_scale EMBEDDED_CFG_SCALE
                        Embedded CFG scale (distilled CFG Scale), default is
                        2.5
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default is
                        3.0.
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --fp8_llm             use fp8 for language model
  --text_encoder_cpu    Inference on CPU for Text Encoder (Qwen2.5-VL)
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,torch,sageattn,xformers,sdpa}
                        attention mode
  --use_32bit_attention
                        use 32-bit precision for attention computations in DiT
                        model even when using mixed precision (original
                        behavior)
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {images,latent,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console
  --bell                Ring bell when done. For interactive mode, ring bell
                        on each iteration. For other modes, ring bell at the
                        end.

============================================================
### hv_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: hv_train_network.py [-h] [--config_file CONFIG_FILE]
                           [--dataset_config DATASET_CONFIG] [--sdpa]
                           [--flash_attn] [--sage_attn] [--xformers]
                           [--flash3] [--split_attn] [--compile]
                           [--compile_backend COMPILE_BACKEND]
                           [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                           [--compile_dynamic {true,false,auto}]
                           [--compile_fullgraph]
                           [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                           [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                           [--max_train_steps MAX_TRAIN_STEPS]
                           [--max_train_epochs MAX_TRAIN_EPOCHS]
                           [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                           [--persistent_data_loader_workers] [--seed SEED]
                           [--gradient_checkpointing]
                           [--gradient_checkpointing_cpu_offload]
                           [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                           [--mixed_precision {no,fp16,bf16}]
                           [--logging_dir LOGGING_DIR]
                           [--log_with {tensorboard,wandb,all}]
                           [--log_prefix LOG_PREFIX]
                           [--log_tracker_name LOG_TRACKER_NAME]
                           [--wandb_run_name WANDB_RUN_NAME]
                           [--log_tracker_config LOG_TRACKER_CONFIG]
                           [--wandb_api_key WANDB_API_KEY] [--log_config]
                           [--ddp_timeout DDP_TIMEOUT]
                           [--ddp_gradient_as_bucket_view]
                           [--ddp_static_graph]
                           [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                           [--sample_at_first]
                           [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                           [--sample_prompts SAMPLE_PROMPTS]
                           [--optimizer_type OPTIMIZER_TYPE]
                           [--optimizer_args [OPTIMIZER_ARGS ...]]
                           [--learning_rate LEARNING_RATE]
                           [--max_grad_norm MAX_GRAD_NORM]
                           [--lr_scheduler LR_SCHEDULER]
                           [--lr_warmup_steps LR_WARMUP_STEPS]
                           [--lr_decay_steps LR_DECAY_STEPS]
                           [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                           [--lr_scheduler_power LR_SCHEDULER_POWER]
                           [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                           [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                           [--lr_scheduler_type LR_SCHEDULER_TYPE]
                           [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                           [--fp8_base]
                           [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                           [--dynamo_mode {default,reduce-overhead,max-autotune}]
                           [--dynamo_fullgraph] [--dynamo_dynamic]
                           [--blocks_to_swap BLOCKS_TO_SWAP]
                           [--use_pinned_memory_for_block_swap]
                           [--img_in_txt_in_offloading]
                           [--disable_numpy_memmap]
                           [--guidance_scale GUIDANCE_SCALE]
                           [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                           [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                           [--sigmoid_scale SIGMOID_SCALE]
                           [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                           [--logit_mean LOGIT_MEAN] [--logit_std LOGIT_STD]
                           [--mode_scale MODE_SCALE]
                           [--min_timestep MIN_TIMESTEP]
                           [--max_timestep MAX_TIMESTEP]
                           [--preserve_distribution_shape]
                           [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                           [--show_timesteps {image,console}] [--no_metadata]
                           [--network_weights NETWORK_WEIGHTS]
                           [--network_module NETWORK_MODULE]
                           [--network_dim NETWORK_DIM]
                           [--network_alpha NETWORK_ALPHA]
                           [--network_dropout NETWORK_DROPOUT]
                           [--network_args [NETWORK_ARGS ...]]
                           [--training_comment TRAINING_COMMENT]
                           [--dim_from_weights]
                           [--scale_weight_norms SCALE_WEIGHT_NORMS]
                           [--base_weights [BASE_WEIGHTS ...]]
                           [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                           [--output_dir OUTPUT_DIR]
                           [--output_name OUTPUT_NAME] [--resume RESUME]
                           [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                           [--save_every_n_steps SAVE_EVERY_N_STEPS]
                           [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                           [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                           [--save_last_n_steps SAVE_LAST_N_STEPS]
                           [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                           [--save_state] [--save_state_on_train_end]
                           [--metadata_title METADATA_TITLE]
                           [--metadata_author METADATA_AUTHOR]
                           [--metadata_description METADATA_DESCRIPTION]
                           [--metadata_license METADATA_LICENSE]
                           [--metadata_tags METADATA_TAGS]
                           [--metadata_reso METADATA_RESO]
                           [--metadata_arch METADATA_ARCH]
                           [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                           [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                           [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                           [--huggingface_token HUGGINGFACE_TOKEN]
                           [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                           [--save_state_to_huggingface]
                           [--resume_from_huggingface] [--async_upload]
                           [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                           [--dit_dtype DIT_DTYPE]
                           [--dit_in_channels DIT_IN_CHANNELS] [--fp8_llm]
                           [--text_encoder1 TEXT_ENCODER1]
                           [--text_encoder2 TEXT_ENCODER2]
                           [--text_encoder_dtype TEXT_ENCODER_DTYPE]
                           [--vae_tiling] [--vae_chunk_size VAE_CHUNK_SIZE]
                           [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --dit_dtype DIT_DTYPE
                        data type for DiT, default is bfloat16
  --dit_in_channels DIT_IN_CHANNELS
                        input channels for DiT, default is 16, skyreels I2V is
                        32
  --fp8_llm             use fp8 for LLM / LLMにfp8を使う
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory / テキストエンコーダ1のディレクトリ
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory / テキストエンコーダ2のディレクトリ
  --text_encoder_dtype TEXT_ENCODER_DTYPE
                        data type for Text Encoder, default is float16
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled. / VAEの空間タイリングを有効にする、デフォルトはFalse
                        。vae_spatial_tile_sample_min_sizeが設定されている場合、自動的に有効になりま
                        す。
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256

============================================================
### hv_train.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: hv_train.py [-h] [--config_file CONFIG_FILE]
                   [--dataset_config DATASET_CONFIG] [--sdpa] [--flash_attn]
                   [--sage_attn] [--xformers] [--split_attn]
                   [--max_train_steps MAX_TRAIN_STEPS]
                   [--max_train_epochs MAX_TRAIN_EPOCHS]
                   [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                   [--persistent_data_loader_workers] [--seed SEED]
                   [--gradient_checkpointing]
                   [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                   [--mixed_precision {no,fp16,bf16}]
                   [--trainable_modules TRAINABLE_MODULES [TRAINABLE_MODULES ...]]
                   [--logging_dir LOGGING_DIR]
                   [--log_with {tensorboard,wandb,all}]
                   [--log_prefix LOG_PREFIX]
                   [--log_tracker_name LOG_TRACKER_NAME]
                   [--wandb_run_name WANDB_RUN_NAME]
                   [--log_tracker_config LOG_TRACKER_CONFIG]
                   [--wandb_api_key WANDB_API_KEY] [--log_config]
                   [--ddp_timeout DDP_TIMEOUT] [--ddp_gradient_as_bucket_view]
                   [--ddp_static_graph]
                   [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                   [--sample_at_first]
                   [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                   [--sample_prompts SAMPLE_PROMPTS]
                   [--optimizer_type OPTIMIZER_TYPE]
                   [--optimizer_args [OPTIMIZER_ARGS ...]]
                   [--learning_rate LEARNING_RATE]
                   [--max_grad_norm MAX_GRAD_NORM]
                   [--lr_scheduler LR_SCHEDULER]
                   [--lr_warmup_steps LR_WARMUP_STEPS]
                   [--lr_decay_steps LR_DECAY_STEPS]
                   [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                   [--lr_scheduler_power LR_SCHEDULER_POWER]
                   [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                   [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                   [--lr_scheduler_type LR_SCHEDULER_TYPE]
                   [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]] [--dit DIT]
                   [--dit_dtype DIT_DTYPE] [--dit_in_channels DIT_IN_CHANNELS]
                   [--vae VAE] [--vae_dtype VAE_DTYPE] [--vae_tiling]
                   [--vae_chunk_size VAE_CHUNK_SIZE]
                   [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]
                   [--text_encoder1 TEXT_ENCODER1]
                   [--text_encoder2 TEXT_ENCODER2]
                   [--text_encoder_dtype TEXT_ENCODER_DTYPE] [--fp8_llm]
                   [--full_fp16] [--full_bf16]
                   [--blocks_to_swap BLOCKS_TO_SWAP]
                   [--img_in_txt_in_offloading]
                   [--guidance_scale GUIDANCE_SCALE]
                   [--timestep_sampling {sigma,uniform,sigmoid,shift}]
                   [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                   [--sigmoid_scale SIGMOID_SCALE]
                   [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                   [--logit_mean LOGIT_MEAN] [--logit_std LOGIT_STD]
                   [--mode_scale MODE_SCALE] [--min_timestep MIN_TIMESTEP]
                   [--max_timestep MAX_TIMESTEP] [--output_dir OUTPUT_DIR]
                   [--output_name OUTPUT_NAME] [--resume RESUME]
                   [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                   [--save_every_n_steps SAVE_EVERY_N_STEPS]
                   [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                   [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                   [--save_last_n_steps SAVE_LAST_N_STEPS]
                   [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                   [--save_state] [--save_state_on_train_end]
                   [--metadata_title METADATA_TITLE]
                   [--metadata_author METADATA_AUTHOR]
                   [--metadata_description METADATA_DESCRIPTION]
                   [--metadata_license METADATA_LICENSE]
                   [--metadata_tags METADATA_TAGS]
                   [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                   [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                   [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                   [--huggingface_token HUGGINGFACE_TOKEN]
                   [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                   [--save_state_to_huggingface] [--resume_from_huggingface]
                   [--async_upload]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --trainable_modules TRAINABLE_MODULES [TRAINABLE_MODULES ...]
                        Enter a list of trainable modules
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler and
                        warmup decay scheduler / 初期学習率の比率としての最小学習率を指定する、cosine
                        with min lr と warmup decay スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --dit_dtype DIT_DTYPE
                        data type for DiT, default is bfloat16
  --dit_in_channels DIT_IN_CHANNELS
                        input channels for DiT, default is 16, skyreels I2V is
                        32
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default is float16
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled. / VAEの空間タイリングを有効にする、デフォルトはFalse
                        。vae_spatial_tile_sample_min_sizeが設定されている場合、自動的に有効になりま
                        す。
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory / テキストエンコーダ1のディレクトリ
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory / テキストエンコーダ2のディレクトリ
  --text_encoder_dtype TEXT_ENCODER_DTYPE
                        data type for Text Encoder, default is float16
  --fp8_llm             use fp8 for LLM / LLMにfp8を使う
  --full_fp16           fp16 training including gradients / 勾配も含めてfp16で学習する
  --full_bf16           bf16 training including gradients / 勾配も含めてbf16で学習する
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale.
  --timestep_sampling {sigma,uniform,sigmoid,shift}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal and shift of sigmoid.
                        / タイムステップをサンプリングする方法：sigma、random uniform、random
                        normalのsigmoid、sigmoidのシフト。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする

============================================================
### hv_generate_video.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: hv_generate_video.py [-h] --dit DIT [--dit_in_channels DIT_IN_CHANNELS]
                            --vae VAE [--vae_dtype VAE_DTYPE] --text_encoder1
                            TEXT_ENCODER1 --text_encoder2 TEXT_ENCODER2
                            [--lora_weight [LORA_WEIGHT ...]]
                            [--lora_multiplier [LORA_MULTIPLIER ...]]
                            [--save_merged_model SAVE_MERGED_MODEL]
                            [--exclude_single_blocks] --prompt PROMPT
                            [--negative_prompt NEGATIVE_PROMPT]
                            [--video_size VIDEO_SIZE VIDEO_SIZE]
                            [--video_length VIDEO_LENGTH] [--fps FPS]
                            [--infer_steps INFER_STEPS] --save_path SAVE_PATH
                            [--seed SEED] [--guidance_scale GUIDANCE_SCALE]
                            [--embedded_cfg_scale EMBEDDED_CFG_SCALE]
                            [--video_path VIDEO_PATH]
                            [--image_path IMAGE_PATH] [--split_uncond]
                            [--strength STRENGTH] [--flow_shift FLOW_SHIFT]
                            [--fp8] [--fp8_llm] [--device DEVICE]
                            [--attn_mode {flash,torch,sageattn,xformers,sdpa}]
                            [--split_attn] [--vae_chunk_size VAE_CHUNK_SIZE]
                            [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]
                            [--blocks_to_swap BLOCKS_TO_SWAP]
                            [--use_pinned_memory_for_block_swap]
                            [--img_in_txt_in_offloading]
                            [--output_type {video,images,latent,both}]
                            [--no_metadata] [--latent_path [LATENT_PATH ...]]
                            [--lycoris] [--fp8_fast]
                            [--compile_args BACKEND MODE DYNAMIC FULLGRAPH]
                            [--compile] [--compile_backend COMPILE_BACKEND]
                            [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                            [--compile_dynamic {true,false,auto}]
                            [--compile_fullgraph]
                            [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]

HunyuanVideo inference script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT checkpoint path or directory
  --dit_in_channels DIT_IN_CHANNELS
                        input channels for DiT, default is None (automatically
                        detect). 32 for SkyReels-I2V, 16 for others
  --vae VAE             VAE checkpoint path or directory
  --vae_dtype VAE_DTYPE
                        data type for VAE, default is float16
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --exclude_single_blocks
                        Exclude single blocks when loading LoRA weights
  --prompt PROMPT       prompt for generation
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation
  --video_size VIDEO_SIZE VIDEO_SIZE
                        video size
  --video_length VIDEO_LENGTH
                        video length
  --fps FPS             video fps
  --infer_steps INFER_STEPS
                        number of inference steps
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        is 1.0 (means no guidance)
  --embedded_cfg_scale EMBEDDED_CFG_SCALE
                        Embeded classifier free guidance scale.
  --video_path VIDEO_PATH
                        path to video for video2video inference
  --image_path IMAGE_PATH
                        path to image for image2video inference, only works
                        for SkyReels-I2V model
  --split_uncond        split unconditional call for classifier free guidance,
                        slower but less memory usage
  --strength STRENGTH   strength for video2video inference
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers.
  --fp8                 use fp8 for DiT model
  --fp8_llm             use fp8 for Text Encoder 1 (LLM)
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,torch,sageattn,xformers,sdpa}
                        attention mode
  --split_attn          use split attention, default is False. if True,
                        --split_uncond becomes True
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu
  --output_type {video,images,latent,both}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --fp8_fast            Enable fast FP8 arthimetic(RTX 4XXX+)
  --compile_args BACKEND MODE DYNAMIC FULLGRAPH
                        [Deprecated] Torch.compile settings. Use individual
                        args instead.
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）

============================================================
### hv_1_5_cache_latents.py
============================================================
W0416 15:05:05.075000 27620 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: hv_1_5_cache_latents.py [-h] --dataset_config DATASET_CONFIG
                               [--vae VAE] [--vae_dtype VAE_DTYPE]
                               [--device DEVICE] [--batch_size BATCH_SIZE]
                               [--num_workers NUM_WORKERS] [--skip_existing]
                               [--keep_cache]
                               [--debug_mode {image,console,video}]
                               [--console_width CONSOLE_WIDTH]
                               [--console_back CONSOLE_BACK]
                               [--console_num_images CONSOLE_NUM_IMAGES]
                               [--disable_cudnn_backend]
                               [--vae_sample_size VAE_SAMPLE_SIZE]
                               [--vae_enable_patch_conv] [--i2v]
                               [--image_encoder IMAGE_ENCODER]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --vae_sample_size VAE_SAMPLE_SIZE
                        VAE sample size (height/width). Default 128; set 256
                        if VRAM is sufficient for better quality; set 0 to
                        disable tiling.
  --vae_enable_patch_conv
                        Enable patch-based convolution in VAE for memory
                        optimization
  --i2v                 Cache image features and conditional latents for I2V
                        training/inference
  --image_encoder IMAGE_ENCODER
                        Directory/path of SigLIP Image Encoder (required if
                        --i2v is set)

============================================================
### hv_1_5_cache_text_encoder_outputs.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: hv_1_5_cache_text_encoder_outputs.py [-h] --dataset_config
                                            DATASET_CONFIG [--device DEVICE]
                                            [--batch_size BATCH_SIZE]
                                            [--num_workers NUM_WORKERS]
                                            [--skip_existing] [--keep_cache]
                                            --text_encoder TEXT_ENCODER --byt5
                                            BYT5 [--fp8_vl]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder TEXT_ENCODER
                        Text Encoder (Qwen2.5-VL) checkpoint path
  --byt5 BYT5           BYT5 text encoder checkpoint path
  --fp8_vl              use fp8 for Qwen2.5-VL model

============================================================
### hv_1_5_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: hv_1_5_train_network.py [-h] [--config_file CONFIG_FILE]
                               [--dataset_config DATASET_CONFIG] [--sdpa]
                               [--flash_attn] [--sage_attn] [--xformers]
                               [--flash3] [--split_attn] [--compile]
                               [--compile_backend COMPILE_BACKEND]
                               [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                               [--compile_dynamic {true,false,auto}]
                               [--compile_fullgraph]
                               [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                               [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                               [--max_train_steps MAX_TRAIN_STEPS]
                               [--max_train_epochs MAX_TRAIN_EPOCHS]
                               [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                               [--persistent_data_loader_workers]
                               [--seed SEED] [--gradient_checkpointing]
                               [--gradient_checkpointing_cpu_offload]
                               [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                               [--mixed_precision {no,fp16,bf16}]
                               [--logging_dir LOGGING_DIR]
                               [--log_with {tensorboard,wandb,all}]
                               [--log_prefix LOG_PREFIX]
                               [--log_tracker_name LOG_TRACKER_NAME]
                               [--wandb_run_name WANDB_RUN_NAME]
                               [--log_tracker_config LOG_TRACKER_CONFIG]
                               [--wandb_api_key WANDB_API_KEY] [--log_config]
                               [--ddp_timeout DDP_TIMEOUT]
                               [--ddp_gradient_as_bucket_view]
                               [--ddp_static_graph]
                               [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                               [--sample_at_first]
                               [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                               [--sample_prompts SAMPLE_PROMPTS]
                               [--optimizer_type OPTIMIZER_TYPE]
                               [--optimizer_args [OPTIMIZER_ARGS ...]]
                               [--learning_rate LEARNING_RATE]
                               [--max_grad_norm MAX_GRAD_NORM]
                               [--lr_scheduler LR_SCHEDULER]
                               [--lr_warmup_steps LR_WARMUP_STEPS]
                               [--lr_decay_steps LR_DECAY_STEPS]
                               [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                               [--lr_scheduler_power LR_SCHEDULER_POWER]
                               [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                               [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                               [--lr_scheduler_type LR_SCHEDULER_TYPE]
                               [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                               [--fp8_base]
                               [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                               [--dynamo_mode {default,reduce-overhead,max-autotune}]
                               [--dynamo_fullgraph] [--dynamo_dynamic]
                               [--blocks_to_swap BLOCKS_TO_SWAP]
                               [--use_pinned_memory_for_block_swap]
                               [--img_in_txt_in_offloading]
                               [--disable_numpy_memmap]
                               [--guidance_scale GUIDANCE_SCALE]
                               [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                               [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                               [--sigmoid_scale SIGMOID_SCALE]
                               [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                               [--logit_mean LOGIT_MEAN]
                               [--logit_std LOGIT_STD]
                               [--mode_scale MODE_SCALE]
                               [--min_timestep MIN_TIMESTEP]
                               [--max_timestep MAX_TIMESTEP]
                               [--preserve_distribution_shape]
                               [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                               [--show_timesteps {image,console}]
                               [--no_metadata]
                               [--network_weights NETWORK_WEIGHTS]
                               [--network_module NETWORK_MODULE]
                               [--network_dim NETWORK_DIM]
                               [--network_alpha NETWORK_ALPHA]
                               [--network_dropout NETWORK_DROPOUT]
                               [--network_args [NETWORK_ARGS ...]]
                               [--training_comment TRAINING_COMMENT]
                               [--dim_from_weights]
                               [--scale_weight_norms SCALE_WEIGHT_NORMS]
                               [--base_weights [BASE_WEIGHTS ...]]
                               [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                               [--output_dir OUTPUT_DIR]
                               [--output_name OUTPUT_NAME] [--resume RESUME]
                               [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                               [--save_every_n_steps SAVE_EVERY_N_STEPS]
                               [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                               [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                               [--save_last_n_steps SAVE_LAST_N_STEPS]
                               [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                               [--save_state] [--save_state_on_train_end]
                               [--metadata_title METADATA_TITLE]
                               [--metadata_author METADATA_AUTHOR]
                               [--metadata_description METADATA_DESCRIPTION]
                               [--metadata_license METADATA_LICENSE]
                               [--metadata_tags METADATA_TAGS]
                               [--metadata_reso METADATA_RESO]
                               [--metadata_arch METADATA_ARCH]
                               [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                               [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                               [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                               [--huggingface_token HUGGINGFACE_TOKEN]
                               [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                               [--save_state_to_huggingface]
                               [--resume_from_huggingface] [--async_upload]
                               [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                               [--task {t2v,i2v}] [--dit_dtype DIT_DTYPE]
                               [--fp8_scaled] [--text_encoder TEXT_ENCODER]
                               [--fp8_vl] [--byt5 BYT5]
                               [--image_encoder IMAGE_ENCODER]
                               [--vae_sample_size VAE_SAMPLE_SIZE]
                               [--vae_enable_patch_conv]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --task {t2v,i2v}      training task type: text-to-video (t2v) or image-to-
                        video (i2v)
  --dit_dtype DIT_DTYPE
                        data type for DiT, default is bfloat16
  --fp8_scaled          use scaled fp8 for DiT
  --text_encoder TEXT_ENCODER
                        text encoder (Qwen2.5-VL) checkpoint path
  --fp8_vl              use fp8 for Text Encoder model
  --byt5 BYT5           BYT5 checkpoint path
  --image_encoder IMAGE_ENCODER
                        SigLIP image encoder path (for I2V cache
                        compatibility)
  --vae_sample_size VAE_SAMPLE_SIZE
                        VAE sample size (height/width). Default 128; set 256
                        if VRAM is sufficient for better quality. Set 0 to
                        disable tiling.
  --vae_enable_patch_conv
                        Enable patch-based convolution in VAE for memory
                        optimization

============================================================
### hv_1_5_generate_video.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: hv_1_5_generate_video.py [-h] [--dit DIT] [--disable_numpy_memmap]
                                [--vae VAE] [--vae_dtype VAE_DTYPE]
                                [--vae_sample_size VAE_SAMPLE_SIZE]
                                [--vae_enable_patch_conv]
                                [--text_encoder TEXT_ENCODER]
                                [--text_encoder_cpu] [--byt5 BYT5]
                                [--image_encoder IMAGE_ENCODER]
                                [--lora_weight [LORA_WEIGHT ...]]
                                [--lora_multiplier [LORA_MULTIPLIER ...]]
                                [--include_patterns [INCLUDE_PATTERNS ...]]
                                [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                                [--save_merged_model SAVE_MERGED_MODEL]
                                [--prompt PROMPT]
                                [--negative_prompt NEGATIVE_PROMPT]
                                [--video_size VIDEO_SIZE VIDEO_SIZE]
                                [--video_length VIDEO_LENGTH] [--fps FPS]
                                [--infer_steps INFER_STEPS] --save_path
                                SAVE_PATH [--seed SEED] [--cpu_noise]
                                [--guidance_scale GUIDANCE_SCALE]
                                [--image_path IMAGE_PATH]
                                [--flow_shift FLOW_SHIFT] [--fp8]
                                [--fp8_scaled] [--device DEVICE]
                                [--attn_mode {flash,flash2,flash3,torch,sageattn,xformers,sdpa}]
                                [--blocks_to_swap BLOCKS_TO_SWAP]
                                [--use_pinned_memory_for_block_swap]
                                [--output_type {video,images,latent,both,latent_images}]
                                [--no_metadata]
                                [--latent_path [LATENT_PATH ...]] [--lycoris]
                                [--compile]
                                [--compile_backend COMPILE_BACKEND]
                                [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                [--compile_dynamic {true,false,auto}]
                                [--compile_fullgraph]
                                [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                [--from_file FROM_FILE] [--interactive]

HunyuanVideo-1.5 inference script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT checkpoint path
  --disable_numpy_memmap
                        Disable numpy memmap when loading safetensors. Default
                        is False.
  --vae VAE             VAE checkpoint path
  --vae_dtype VAE_DTYPE
                        data type for VAE, default is float16. If VRAM is
                        sufficient, use float32 for better quality.
  --vae_sample_size VAE_SAMPLE_SIZE
                        VAE sample size (height and width). Default is 128. If
                        VRAM is sufficient, set to 256 for better quality. Set
                        to 0 to disable tiling.
  --vae_enable_patch_conv
                        Enable patch-based convolution in VAE for memory
                        optimization
  --text_encoder TEXT_ENCODER
                        Text encoder checkpoint path (Qwen2.5-VL)
  --text_encoder_cpu    Load text encoder on CPU to save GPU memory
  --byt5 BYT5           BYT5 text encoder checkpoint path
  --image_encoder IMAGE_ENCODER
                        Image Encoder directory or path
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --prompt PROMPT       prompt for generation
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation, use default negative
                        prompt if not specified
  --video_size VIDEO_SIZE VIDEO_SIZE
                        video size, height and width
  --video_length VIDEO_LENGTH
                        video length, required
  --fps FPS             video fps, Default is 24
  --infer_steps INFER_STEPS
                        number of inference steps, default is 50
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --cpu_noise           Use CPU to generate noise (compatible with ComfyUI).
                        Default is False.
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        is 6.0.
  --image_path IMAGE_PATH
                        Path to image for image2video inference. If not
                        specified, text2video is used.
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,flash2,flash3,torch,sageattn,xformers,sdpa}
                        attention mode
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {video,images,latent,both,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console

============================================================
### fpack_cache_latents.py
============================================================
W0416 15:05:22.590000 31180 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: fpack_cache_latents.py [-h] --dataset_config DATASET_CONFIG [--vae VAE]
                              [--vae_dtype VAE_DTYPE] [--device DEVICE]
                              [--batch_size BATCH_SIZE]
                              [--num_workers NUM_WORKERS] [--skip_existing]
                              [--keep_cache]
                              [--debug_mode {image,console,video}]
                              [--console_width CONSOLE_WIDTH]
                              [--console_back CONSOLE_BACK]
                              [--console_num_images CONSOLE_NUM_IMAGES]
                              [--disable_cudnn_backend] [--vae_tiling]
                              [--vae_chunk_size VAE_CHUNK_SIZE]
                              [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]
                              --image_encoder IMAGE_ENCODER [--f1]
                              [--one_frame] [--one_frame_no_2x]
                              [--one_frame_no_4x]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256
  --image_encoder IMAGE_ENCODER
                        Image encoder (CLIP) checkpoint path or directory
  --f1                  Generate cache for F1 model (vanilla (autoregressive)
                        sampling) instead of Inverted anti-drifting (plain
                        FramePack)
  --one_frame           Generate cache for one frame training (single frame,
                        single section). latent_window_size is used as the
                        index of the target frame.
  --one_frame_no_2x     Do not use clean_latents_2x and
                        clean_latent_2x_indices for one frame training.
  --one_frame_no_4x     Do not use clean_latents_4x and
                        clean_latent_4x_indices for one frame training.

============================================================
### fpack_cache_text_encoder_outputs.py
============================================================
W0416 15:05:27.869000 16756 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: fpack_cache_text_encoder_outputs.py [-h] --dataset_config
                                           DATASET_CONFIG [--device DEVICE]
                                           [--batch_size BATCH_SIZE]
                                           [--num_workers NUM_WORKERS]
                                           [--skip_existing] [--keep_cache]
                                           --text_encoder1 TEXT_ENCODER1
                                           --text_encoder2 TEXT_ENCODER2
                                           [--fp8_llm]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory
  --fp8_llm             use fp8 for Text Encoder 1 (LLM)

============================================================
### fpack_train_network.py
============================================================
W0416 15:05:33.137000 42872 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
Xformers is not installed!
Flash Attn is not installed!
Sage Attn is not installed!
Trying to import sageattention
Failed to import sageattention
usage: fpack_train_network.py [-h] [--config_file CONFIG_FILE]
                              [--dataset_config DATASET_CONFIG] [--sdpa]
                              [--flash_attn] [--sage_attn] [--xformers]
                              [--flash3] [--split_attn] [--compile]
                              [--compile_backend COMPILE_BACKEND]
                              [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                              [--compile_dynamic {true,false,auto}]
                              [--compile_fullgraph]
                              [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                              [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                              [--max_train_steps MAX_TRAIN_STEPS]
                              [--max_train_epochs MAX_TRAIN_EPOCHS]
                              [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                              [--persistent_data_loader_workers] [--seed SEED]
                              [--gradient_checkpointing]
                              [--gradient_checkpointing_cpu_offload]
                              [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                              [--mixed_precision {no,fp16,bf16}]
                              [--logging_dir LOGGING_DIR]
                              [--log_with {tensorboard,wandb,all}]
                              [--log_prefix LOG_PREFIX]
                              [--log_tracker_name LOG_TRACKER_NAME]
                              [--wandb_run_name WANDB_RUN_NAME]
                              [--log_tracker_config LOG_TRACKER_CONFIG]
                              [--wandb_api_key WANDB_API_KEY] [--log_config]
                              [--ddp_timeout DDP_TIMEOUT]
                              [--ddp_gradient_as_bucket_view]
                              [--ddp_static_graph]
                              [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                              [--sample_at_first]
                              [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                              [--sample_prompts SAMPLE_PROMPTS]
                              [--optimizer_type OPTIMIZER_TYPE]
                              [--optimizer_args [OPTIMIZER_ARGS ...]]
                              [--learning_rate LEARNING_RATE]
                              [--max_grad_norm MAX_GRAD_NORM]
                              [--lr_scheduler LR_SCHEDULER]
                              [--lr_warmup_steps LR_WARMUP_STEPS]
                              [--lr_decay_steps LR_DECAY_STEPS]
                              [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                              [--lr_scheduler_power LR_SCHEDULER_POWER]
                              [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                              [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                              [--lr_scheduler_type LR_SCHEDULER_TYPE]
                              [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                              [--fp8_base]
                              [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                              [--dynamo_mode {default,reduce-overhead,max-autotune}]
                              [--dynamo_fullgraph] [--dynamo_dynamic]
                              [--blocks_to_swap BLOCKS_TO_SWAP]
                              [--use_pinned_memory_for_block_swap]
                              [--img_in_txt_in_offloading]
                              [--disable_numpy_memmap]
                              [--guidance_scale GUIDANCE_SCALE]
                              [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                              [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                              [--sigmoid_scale SIGMOID_SCALE]
                              [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                              [--logit_mean LOGIT_MEAN]
                              [--logit_std LOGIT_STD]
                              [--mode_scale MODE_SCALE]
                              [--min_timestep MIN_TIMESTEP]
                              [--max_timestep MAX_TIMESTEP]
                              [--preserve_distribution_shape]
                              [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                              [--show_timesteps {image,console}]
                              [--no_metadata]
                              [--network_weights NETWORK_WEIGHTS]
                              [--network_module NETWORK_MODULE]
                              [--network_dim NETWORK_DIM]
                              [--network_alpha NETWORK_ALPHA]
                              [--network_dropout NETWORK_DROPOUT]
                              [--network_args [NETWORK_ARGS ...]]
                              [--training_comment TRAINING_COMMENT]
                              [--dim_from_weights]
                              [--scale_weight_norms SCALE_WEIGHT_NORMS]
                              [--base_weights [BASE_WEIGHTS ...]]
                              [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                              [--output_dir OUTPUT_DIR]
                              [--output_name OUTPUT_NAME] [--resume RESUME]
                              [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                              [--save_every_n_steps SAVE_EVERY_N_STEPS]
                              [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                              [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                              [--save_last_n_steps SAVE_LAST_N_STEPS]
                              [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                              [--save_state] [--save_state_on_train_end]
                              [--metadata_title METADATA_TITLE]
                              [--metadata_author METADATA_AUTHOR]
                              [--metadata_description METADATA_DESCRIPTION]
                              [--metadata_license METADATA_LICENSE]
                              [--metadata_tags METADATA_TAGS]
                              [--metadata_reso METADATA_RESO]
                              [--metadata_arch METADATA_ARCH]
                              [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                              [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                              [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                              [--huggingface_token HUGGINGFACE_TOKEN]
                              [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                              [--save_state_to_huggingface]
                              [--resume_from_huggingface] [--async_upload]
                              [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                              [--fp8_scaled] [--fp8_llm]
                              [--text_encoder1 TEXT_ENCODER1]
                              [--text_encoder2 TEXT_ENCODER2] [--vae_tiling]
                              [--vae_chunk_size VAE_CHUNK_SIZE]
                              [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]
                              [--image_encoder IMAGE_ENCODER]
                              [--latent_window_size LATENT_WINDOW_SIZE]
                              [--bulk_decode] [--f1] [--one_frame]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --fp8_llm             use fp8 for LLM / LLMにfp8を使う
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory / テキストエンコーダ1のディレクトリ
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory / テキストエンコーダ2のディレクトリ
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256
  --image_encoder IMAGE_ENCODER
                        Image encoder (CLIP) checkpoint path or directory
  --latent_window_size LATENT_WINDOW_SIZE
                        FramePack latent window size (default 9)
  --bulk_decode         decode all frames at once in sample generation
  --f1                  Use F1 sampling method for sample generation
  --one_frame           Use one frame sampling method for sample generation

============================================================
### fpack_generate_video.py
============================================================
W0416 15:05:38.394000 47020 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
Xformers is not installed!
Flash Attn is not installed!
Sage Attn is not installed!
Trying to import sageattention
Failed to import sageattention
usage: fpack_generate_video.py [-h] [--sample_solver {unipc,dpm++,vanilla}]
                               [--dit DIT] [--disable_numpy_memmap]
                               [--vae VAE] --text_encoder1 TEXT_ENCODER1
                               --text_encoder2 TEXT_ENCODER2 --image_encoder
                               IMAGE_ENCODER [--f1]
                               [--lora_weight [LORA_WEIGHT ...]]
                               [--lora_multiplier [LORA_MULTIPLIER ...]]
                               [--include_patterns [INCLUDE_PATTERNS ...]]
                               [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                               [--save_merged_model SAVE_MERGED_MODEL]
                               [--prompt PROMPT]
                               [--negative_prompt NEGATIVE_PROMPT]
                               [--custom_system_prompt CUSTOM_SYSTEM_PROMPT]
                               [--video_size VIDEO_SIZE VIDEO_SIZE]
                               [--video_seconds VIDEO_SECONDS]
                               [--video_sections VIDEO_SECTIONS]
                               [--one_frame_inference ONE_FRAME_INFERENCE]
                               [--one_frame_auto_resize]
                               [--control_image_path [CONTROL_IMAGE_PATH ...]]
                               [--control_image_mask_path [CONTROL_IMAGE_MASK_PATH ...]]
                               [--fps FPS] [--infer_steps INFER_STEPS]
                               --save_path SAVE_PATH [--seed SEED]
                               [--latent_window_size LATENT_WINDOW_SIZE]
                               [--embedded_cfg_scale EMBEDDED_CFG_SCALE]
                               [--guidance_scale GUIDANCE_SCALE]
                               [--guidance_rescale GUIDANCE_RESCALE]
                               [--image_path IMAGE_PATH]
                               [--end_image_path END_IMAGE_PATH]
                               [--latent_paddings LATENT_PADDINGS]
                               [--flow_shift FLOW_SHIFT] [--fp8]
                               [--fp8_scaled]
                               [--rope_scaling_factor ROPE_SCALING_FACTOR]
                               [--rope_scaling_timestep_threshold ROPE_SCALING_TIMESTEP_THRESHOLD]
                               [--fp8_llm] [--device DEVICE]
                               [--attn_mode {flash,torch,sageattn,xformers,sdpa}]
                               [--vae_tiling]
                               [--vae_chunk_size VAE_CHUNK_SIZE]
                               [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]
                               [--bulk_decode]
                               [--blocks_to_swap BLOCKS_TO_SWAP]
                               [--use_pinned_memory_for_block_swap]
                               [--output_type {video,images,latent,both,latent_images}]
                               [--no_metadata]
                               [--latent_path [LATENT_PATH ...]] [--lycoris]
                               [--compile] [--compile_backend COMPILE_BACKEND]
                               [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                               [--compile_dynamic {true,false,auto}]
                               [--compile_fullgraph]
                               [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                               [--magcache_mag_ratios MAGCACHE_MAG_RATIOS]
                               [--magcache_retention_ratio MAGCACHE_RETENTION_RATIO]
                               [--magcache_threshold MAGCACHE_THRESHOLD]
                               [--magcache_k MAGCACHE_K]
                               [--magcache_calibration]
                               [--from_file FROM_FILE] [--interactive]

FramePack inference script

options:
  -h, --help            show this help message and exit
  --sample_solver {unipc,dpm++,vanilla}
                        The solver used to sample.
  --dit DIT             DiT directory or path
  --disable_numpy_memmap
                        Disable numpy memmap when loading safetensors. Default
                        is False.
  --vae VAE             VAE directory or path
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 directory or path
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 directory or path
  --image_encoder IMAGE_ENCODER
                        Image Encoder directory or path
  --f1                  Use F1 sampling method
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --prompt PROMPT       prompt for generation. If `;;;` is used, it will be
                        split into sections. Example: `section_index:prompt`
                        or
                        `section_index:prompt;;;section_index:prompt;;;...`,
                        section_index can be `0` or `-1` or `0-2`, `-1` means
                        last section, `0-2` means from 0 to 2 (inclusive).
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation, default is empty
                        string. should not change.
  --custom_system_prompt CUSTOM_SYSTEM_PROMPT
                        Custom system prompt for LLM. If specified, it will
                        override the default system prompt. See
                        hunyuan_model/text_encoder.py for the default system
                        prompt.
  --video_size VIDEO_SIZE VIDEO_SIZE
                        video size, height and width
  --video_seconds VIDEO_SECONDS
                        video length, default is 5.0 seconds
  --video_sections VIDEO_SECTIONS
                        number of video sections, Default is None (auto
                        calculate from video seconds)
  --one_frame_inference ONE_FRAME_INFERENCE
                        one frame inference, default is None, comma separated
                        values from 'no_2x', 'no_4x', 'no_post',
                        'control_indices' and 'target_index'.
  --one_frame_auto_resize
                        Automatically adjust height and width based on control
                        image size and given size for one frame inference.
                        Default is False.
  --control_image_path [CONTROL_IMAGE_PATH ...]
                        path to control (reference) image for one frame
                        inference.
  --control_image_mask_path [CONTROL_IMAGE_MASK_PATH ...]
                        path to control (reference) image mask for one frame
                        inference.
  --fps FPS             video fps, default is 30
  --infer_steps INFER_STEPS
                        number of inference steps, default is 25
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --latent_window_size LATENT_WINDOW_SIZE
                        latent window size, default is 9. should not change.
  --embedded_cfg_scale EMBEDDED_CFG_SCALE
                        Embeded CFG scale (distilled CFG Scale), default is
                        10.0
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        is 1.0 (no guidance), should not change.
  --guidance_rescale GUIDANCE_RESCALE
                        CFG Re-scale, default is 0.0. Should not change.
  --image_path IMAGE_PATH
                        path to image for image2video inference. If `;;;` is
                        used, it will be used as section images. The notation
                        is same as `--prompt`.
  --end_image_path END_IMAGE_PATH
                        path to end image for image2video inference
  --latent_paddings LATENT_PADDINGS
                        latent paddings for each section, comma separated
                        values. default is None (FramePack default paddings)
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default is
                        None (FramePack default).
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --rope_scaling_factor ROPE_SCALING_FACTOR
                        RoPE scaling factor for high resolution (H/W), default
                        is 0.5
  --rope_scaling_timestep_threshold ROPE_SCALING_TIMESTEP_THRESHOLD
                        RoPE scaling timestep threshold, default is None
                        (disable), if set, RoPE scaling will be applied only
                        for timesteps >= threshold, around 800 is good
                        starting point
  --fp8_llm             use fp8 for Text Encoder 1 (LLM)
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,torch,sageattn,xformers,sdpa}
                        attention mode
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256
  --bulk_decode         decode all frames at once
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {video,images,latent,both,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --magcache_mag_ratios MAGCACHE_MAG_RATIOS
                        Enable MagCache for inference with specified ratios,
                        comma separated values. Example:
                        `1.0,1.06971,1.29073,...`. It is recommended to use
                        same count of ratios as as inference steps.Default is
                        None (disabled), if `0` is specified, it will use
                        default ratios for 50 steps.
  --magcache_retention_ratio MAGCACHE_RETENTION_RATIO
                        MagCache retention ratio, default is 0.2
  --magcache_threshold MAGCACHE_THRESHOLD
                        MagCache threshold, default is 0.24
  --magcache_k MAGCACHE_K
                        MagCache k value, default is 6
  --magcache_calibration
                        Enable MagCache calibration
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console

============================================================
### flux_kontext_cache_latents.py
============================================================
W0416 15:05:43.720000 61040 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
Trying to import sageattention
Failed to import sageattention
usage: flux_kontext_cache_latents.py [-h] --dataset_config DATASET_CONFIG
                                     [--vae VAE] [--vae_dtype VAE_DTYPE]
                                     [--device DEVICE]
                                     [--batch_size BATCH_SIZE]
                                     [--num_workers NUM_WORKERS]
                                     [--skip_existing] [--keep_cache]
                                     [--debug_mode {image,console,video}]
                                     [--console_width CONSOLE_WIDTH]
                                     [--console_back CONSOLE_BACK]
                                     [--console_num_images CONSOLE_NUM_IMAGES]
                                     [--disable_cudnn_backend] [--vae_tiling]
                                     [--vae_chunk_size VAE_CHUNK_SIZE]
                                     [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256

============================================================
### flux_kontext_cache_text_encoder_outputs.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: flux_kontext_cache_text_encoder_outputs.py [-h] --dataset_config
                                                  DATASET_CONFIG
                                                  [--device DEVICE]
                                                  [--batch_size BATCH_SIZE]
                                                  [--num_workers NUM_WORKERS]
                                                  [--skip_existing]
                                                  [--keep_cache]
                                                  --text_encoder1
                                                  TEXT_ENCODER1
                                                  --text_encoder2
                                                  TEXT_ENCODER2 [--fp8_t5]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder1 TEXT_ENCODER1
                        text encoder (T5XXL) checkpoint path
  --text_encoder2 TEXT_ENCODER2
                        text encoder 2 (CLIP-L) checkpoint path
  --fp8_t5              use fp8 for Text Encoder model

============================================================
### flux_kontext_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: flux_kontext_train_network.py [-h] [--config_file CONFIG_FILE]
                                     [--dataset_config DATASET_CONFIG]
                                     [--sdpa] [--flash_attn] [--sage_attn]
                                     [--xformers] [--flash3] [--split_attn]
                                     [--compile]
                                     [--compile_backend COMPILE_BACKEND]
                                     [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                     [--compile_dynamic {true,false,auto}]
                                     [--compile_fullgraph]
                                     [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                     [--cuda_allow_tf32]
                                     [--cuda_cudnn_benchmark]
                                     [--max_train_steps MAX_TRAIN_STEPS]
                                     [--max_train_epochs MAX_TRAIN_EPOCHS]
                                     [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                                     [--persistent_data_loader_workers]
                                     [--seed SEED] [--gradient_checkpointing]
                                     [--gradient_checkpointing_cpu_offload]
                                     [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                                     [--mixed_precision {no,fp16,bf16}]
                                     [--logging_dir LOGGING_DIR]
                                     [--log_with {tensorboard,wandb,all}]
                                     [--log_prefix LOG_PREFIX]
                                     [--log_tracker_name LOG_TRACKER_NAME]
                                     [--wandb_run_name WANDB_RUN_NAME]
                                     [--log_tracker_config LOG_TRACKER_CONFIG]
                                     [--wandb_api_key WANDB_API_KEY]
                                     [--log_config]
                                     [--ddp_timeout DDP_TIMEOUT]
                                     [--ddp_gradient_as_bucket_view]
                                     [--ddp_static_graph]
                                     [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                                     [--sample_at_first]
                                     [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                                     [--sample_prompts SAMPLE_PROMPTS]
                                     [--optimizer_type OPTIMIZER_TYPE]
                                     [--optimizer_args [OPTIMIZER_ARGS ...]]
                                     [--learning_rate LEARNING_RATE]
                                     [--max_grad_norm MAX_GRAD_NORM]
                                     [--lr_scheduler LR_SCHEDULER]
                                     [--lr_warmup_steps LR_WARMUP_STEPS]
                                     [--lr_decay_steps LR_DECAY_STEPS]
                                     [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                                     [--lr_scheduler_power LR_SCHEDULER_POWER]
                                     [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                                     [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                                     [--lr_scheduler_type LR_SCHEDULER_TYPE]
                                     [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                                     [--fp8_base]
                                     [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                                     [--dynamo_mode {default,reduce-overhead,max-autotune}]
                                     [--dynamo_fullgraph] [--dynamo_dynamic]
                                     [--blocks_to_swap BLOCKS_TO_SWAP]
                                     [--use_pinned_memory_for_block_swap]
                                     [--img_in_txt_in_offloading]
                                     [--disable_numpy_memmap]
                                     [--guidance_scale GUIDANCE_SCALE]
                                     [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                                     [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                                     [--sigmoid_scale SIGMOID_SCALE]
                                     [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                                     [--logit_mean LOGIT_MEAN]
                                     [--logit_std LOGIT_STD]
                                     [--mode_scale MODE_SCALE]
                                     [--min_timestep MIN_TIMESTEP]
                                     [--max_timestep MAX_TIMESTEP]
                                     [--preserve_distribution_shape]
                                     [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                                     [--show_timesteps {image,console}]
                                     [--no_metadata]
                                     [--network_weights NETWORK_WEIGHTS]
                                     [--network_module NETWORK_MODULE]
                                     [--network_dim NETWORK_DIM]
                                     [--network_alpha NETWORK_ALPHA]
                                     [--network_dropout NETWORK_DROPOUT]
                                     [--network_args [NETWORK_ARGS ...]]
                                     [--training_comment TRAINING_COMMENT]
                                     [--dim_from_weights]
                                     [--scale_weight_norms SCALE_WEIGHT_NORMS]
                                     [--base_weights [BASE_WEIGHTS ...]]
                                     [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                                     [--output_dir OUTPUT_DIR]
                                     [--output_name OUTPUT_NAME]
                                     [--resume RESUME]
                                     [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                                     [--save_every_n_steps SAVE_EVERY_N_STEPS]
                                     [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                                     [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                                     [--save_last_n_steps SAVE_LAST_N_STEPS]
                                     [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                                     [--save_state]
                                     [--save_state_on_train_end]
                                     [--metadata_title METADATA_TITLE]
                                     [--metadata_author METADATA_AUTHOR]
                                     [--metadata_description METADATA_DESCRIPTION]
                                     [--metadata_license METADATA_LICENSE]
                                     [--metadata_tags METADATA_TAGS]
                                     [--metadata_reso METADATA_RESO]
                                     [--metadata_arch METADATA_ARCH]
                                     [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                                     [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                                     [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                                     [--huggingface_token HUGGINGFACE_TOKEN]
                                     [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                                     [--save_state_to_huggingface]
                                     [--resume_from_huggingface]
                                     [--async_upload] [--dit DIT] [--vae VAE]
                                     [--vae_dtype VAE_DTYPE] [--fp8_scaled]
                                     [--text_encoder1 TEXT_ENCODER1]
                                     [--fp8_t5]
                                     [--text_encoder2 TEXT_ENCODER2]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --text_encoder1 TEXT_ENCODER1
                        text encoder (T5) checkpoint path
  --fp8_t5              use fp8 for Text Encoder model
  --text_encoder2 TEXT_ENCODER2
                        text encoder (CLIP) checkpoint path, optional. If
                        training I2V model, this is required

============================================================
### flux_kontext_generate_image.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: flux_kontext_generate_image.py [-h] [--dit DIT] [--vae VAE]
                                      --text_encoder1 TEXT_ENCODER1
                                      --text_encoder2 TEXT_ENCODER2
                                      [--lora_weight [LORA_WEIGHT ...]]
                                      [--lora_multiplier [LORA_MULTIPLIER ...]]
                                      [--include_patterns [INCLUDE_PATTERNS ...]]
                                      [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                                      [--save_merged_model SAVE_MERGED_MODEL]
                                      [--prompt PROMPT]
                                      [--image_size IMAGE_SIZE IMAGE_SIZE]
                                      [--control_image_path CONTROL_IMAGE_PATH]
                                      [--no_resize_control]
                                      [--infer_steps INFER_STEPS] --save_path
                                      SAVE_PATH [--seed SEED]
                                      [--embedded_cfg_scale EMBEDDED_CFG_SCALE]
                                      [--flow_shift FLOW_SHIFT] [--fp8]
                                      [--fp8_scaled] [--fp8_t5]
                                      [--device DEVICE]
                                      [--attn_mode {flash,torch,sageattn,xformers,sdpa}]
                                      [--blocks_to_swap BLOCKS_TO_SWAP]
                                      [--use_pinned_memory_for_block_swap]
                                      [--output_type {images,latent,latent_images}]
                                      [--no_metadata]
                                      [--latent_path [LATENT_PATH ...]]
                                      [--lycoris] [--compile]
                                      [--compile_backend COMPILE_BACKEND]
                                      [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                      [--compile_dynamic {true,false,auto}]
                                      [--compile_fullgraph]
                                      [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                      [--from_file FROM_FILE] [--interactive]

FLUX.1 Kontext inference script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT directory or path
  --vae VAE             AE directory or path
  --text_encoder1 TEXT_ENCODER1
                        Text Encoder 1 (T5) directory or path
  --text_encoder2 TEXT_ENCODER2
                        Text Encoder 2 (CLIP-L) directory or path
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --prompt PROMPT       prompt for generation
  --image_size IMAGE_SIZE IMAGE_SIZE
                        image size, height and width
  --control_image_path CONTROL_IMAGE_PATH
                        path to control (reference) image for Kontext.
  --no_resize_control   do not resize control image
  --infer_steps INFER_STEPS
                        number of inference steps, default is 25
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --embedded_cfg_scale EMBEDDED_CFG_SCALE
                        Embeded CFG scale (distilled CFG Scale), default is
                        2.5
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default is
                        None (FLUX.1 default).
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --fp8_t5              use fp8 for Text Encoder 1 (T5)
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,torch,sageattn,xformers,sdpa}
                        attention mode
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {images,latent,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console

============================================================
### qwen_image_cache_latents.py
============================================================
W0416 15:06:03.074000 51404 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
Trying to import sageattention
Failed to import sageattention
usage: qwen_image_cache_latents.py [-h] --dataset_config DATASET_CONFIG
                                   [--vae VAE] [--vae_dtype VAE_DTYPE]
                                   [--device DEVICE] [--batch_size BATCH_SIZE]
                                   [--num_workers NUM_WORKERS]
                                   [--skip_existing] [--keep_cache]
                                   [--debug_mode {image,console,video}]
                                   [--console_width CONSOLE_WIDTH]
                                   [--console_back CONSOLE_BACK]
                                   [--console_num_images CONSOLE_NUM_IMAGES]
                                   [--disable_cudnn_backend] [--vae_tiling]
                                   [--vae_chunk_size VAE_CHUNK_SIZE]
                                   [--vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE]
                                   [--edit] [--edit_plus]
                                   [--model_version MODEL_VERSION]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --vae_tiling          enable spatial tiling for VAE, default is False. If
                        vae_spatial_tile_sample_min_size is set, this is
                        automatically enabled
  --vae_chunk_size VAE_CHUNK_SIZE
                        chunk size for CausalConv3d in VAE
  --vae_spatial_tile_sample_min_size VAE_SPATIAL_TILE_SAMPLE_MIN_SIZE
                        spatial tile sample min size for VAE, default 256
  --edit                Enable Qwen-Image-Edit original, recommend
                        `--model_version edit` instead
  --edit_plus           Enable Qwen-Image-Edit-2509 (plus), recommend
                        `--model_version edit-2509` instead
  --model_version MODEL_VERSION
                        training for Qwen-Image model version, e.g.,
                        'original', 'layered', 'edit', 'edit-2509',
                        'edit-2511' etc.

============================================================
### qwen_image_cache_text_encoder_outputs.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: qwen_image_cache_text_encoder_outputs.py [-h] --dataset_config
                                                DATASET_CONFIG
                                                [--device DEVICE]
                                                [--batch_size BATCH_SIZE]
                                                [--num_workers NUM_WORKERS]
                                                [--skip_existing]
                                                [--keep_cache] --text_encoder
                                                TEXT_ENCODER [--fp8_vl]
                                                [--edit] [--edit_plus]
                                                [--model_version MODEL_VERSION]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder TEXT_ENCODER
                        Text Encoder (Qwen2.5-VL) checkpoint path
  --fp8_vl              use fp8 for Text Encoder model
  --edit                Enable Qwen-Image-Edit original, recommend
                        `--model_version edit` instead
  --edit_plus           Enable Qwen-Image-Edit-2509 (plus), recommend
                        `--model_version edit-2509` instead
  --model_version MODEL_VERSION
                        training for Qwen-Image model version, e.g.,
                        'original', 'layered', 'edit', 'edit-2509',
                        'edit-2511' etc.

============================================================
### qwen_image_train_network.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: qwen_image_train_network.py [-h] [--config_file CONFIG_FILE]
                                   [--dataset_config DATASET_CONFIG] [--sdpa]
                                   [--flash_attn] [--sage_attn] [--xformers]
                                   [--flash3] [--split_attn] [--compile]
                                   [--compile_backend COMPILE_BACKEND]
                                   [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                   [--compile_dynamic {true,false,auto}]
                                   [--compile_fullgraph]
                                   [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                   [--cuda_allow_tf32]
                                   [--cuda_cudnn_benchmark]
                                   [--max_train_steps MAX_TRAIN_STEPS]
                                   [--max_train_epochs MAX_TRAIN_EPOCHS]
                                   [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                                   [--persistent_data_loader_workers]
                                   [--seed SEED] [--gradient_checkpointing]
                                   [--gradient_checkpointing_cpu_offload]
                                   [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                                   [--mixed_precision {no,fp16,bf16}]
                                   [--logging_dir LOGGING_DIR]
                                   [--log_with {tensorboard,wandb,all}]
                                   [--log_prefix LOG_PREFIX]
                                   [--log_tracker_name LOG_TRACKER_NAME]
                                   [--wandb_run_name WANDB_RUN_NAME]
                                   [--log_tracker_config LOG_TRACKER_CONFIG]
                                   [--wandb_api_key WANDB_API_KEY]
                                   [--log_config] [--ddp_timeout DDP_TIMEOUT]
                                   [--ddp_gradient_as_bucket_view]
                                   [--ddp_static_graph]
                                   [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                                   [--sample_at_first]
                                   [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                                   [--sample_prompts SAMPLE_PROMPTS]
                                   [--optimizer_type OPTIMIZER_TYPE]
                                   [--optimizer_args [OPTIMIZER_ARGS ...]]
                                   [--learning_rate LEARNING_RATE]
                                   [--max_grad_norm MAX_GRAD_NORM]
                                   [--lr_scheduler LR_SCHEDULER]
                                   [--lr_warmup_steps LR_WARMUP_STEPS]
                                   [--lr_decay_steps LR_DECAY_STEPS]
                                   [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                                   [--lr_scheduler_power LR_SCHEDULER_POWER]
                                   [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                                   [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                                   [--lr_scheduler_type LR_SCHEDULER_TYPE]
                                   [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                                   [--fp8_base]
                                   [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                                   [--dynamo_mode {default,reduce-overhead,max-autotune}]
                                   [--dynamo_fullgraph] [--dynamo_dynamic]
                                   [--blocks_to_swap BLOCKS_TO_SWAP]
                                   [--use_pinned_memory_for_block_swap]
                                   [--img_in_txt_in_offloading]
                                   [--disable_numpy_memmap]
                                   [--guidance_scale GUIDANCE_SCALE]
                                   [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                                   [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                                   [--sigmoid_scale SIGMOID_SCALE]
                                   [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                                   [--logit_mean LOGIT_MEAN]
                                   [--logit_std LOGIT_STD]
                                   [--mode_scale MODE_SCALE]
                                   [--min_timestep MIN_TIMESTEP]
                                   [--max_timestep MAX_TIMESTEP]
                                   [--preserve_distribution_shape]
                                   [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                                   [--show_timesteps {image,console}]
                                   [--no_metadata]
                                   [--network_weights NETWORK_WEIGHTS]
                                   [--network_module NETWORK_MODULE]
                                   [--network_dim NETWORK_DIM]
                                   [--network_alpha NETWORK_ALPHA]
                                   [--network_dropout NETWORK_DROPOUT]
                                   [--network_args [NETWORK_ARGS ...]]
                                   [--training_comment TRAINING_COMMENT]
                                   [--dim_from_weights]
                                   [--scale_weight_norms SCALE_WEIGHT_NORMS]
                                   [--base_weights [BASE_WEIGHTS ...]]
                                   [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                                   [--output_dir OUTPUT_DIR]
                                   [--output_name OUTPUT_NAME]
                                   [--resume RESUME]
                                   [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                                   [--save_every_n_steps SAVE_EVERY_N_STEPS]
                                   [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                                   [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                                   [--save_last_n_steps SAVE_LAST_N_STEPS]
                                   [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                                   [--save_state] [--save_state_on_train_end]
                                   [--metadata_title METADATA_TITLE]
                                   [--metadata_author METADATA_AUTHOR]
                                   [--metadata_description METADATA_DESCRIPTION]
                                   [--metadata_license METADATA_LICENSE]
                                   [--metadata_tags METADATA_TAGS]
                                   [--metadata_reso METADATA_RESO]
                                   [--metadata_arch METADATA_ARCH]
                                   [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                                   [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                                   [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                                   [--huggingface_token HUGGINGFACE_TOKEN]
                                   [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                                   [--save_state_to_huggingface]
                                   [--resume_from_huggingface]
                                   [--async_upload] [--dit DIT] [--vae VAE]
                                   [--vae_dtype VAE_DTYPE] [--fp8_scaled]
                                   [--text_encoder TEXT_ENCODER] [--fp8_vl]
                                   [--num_layers NUM_LAYERS]
                                   [--remove_first_image_from_target] [--edit]
                                   [--edit_plus]
                                   [--model_version MODEL_VERSION]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --text_encoder TEXT_ENCODER
                        text encoder (Qwen2.5-VL) checkpoint path
  --fp8_vl              use fp8 for Text Encoder model
  --num_layers NUM_LAYERS
                        Number of layers in the DiT model, default is None
                        (60)
  --remove_first_image_from_target
                        Remove the first image from the target images for
                        layered model. / レイヤードモデルでターゲット画像から最初の画像を削除する。
  --edit                Enable Qwen-Image-Edit original, recommend
                        `--model_version edit` instead
  --edit_plus           Enable Qwen-Image-Edit-2509 (plus), recommend
                        `--model_version edit-2509` instead
  --model_version MODEL_VERSION
                        training for Qwen-Image model version, e.g.,
                        'original', 'layered', 'edit', 'edit-2509',
                        'edit-2511' etc.

============================================================
### qwen_image_train.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: qwen_image_train.py [-h] [--config_file CONFIG_FILE]
                           [--dataset_config DATASET_CONFIG] [--sdpa]
                           [--flash_attn] [--sage_attn] [--xformers]
                           [--flash3] [--split_attn] [--compile]
                           [--compile_backend COMPILE_BACKEND]
                           [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                           [--compile_dynamic {true,false,auto}]
                           [--compile_fullgraph]
                           [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                           [--cuda_allow_tf32] [--cuda_cudnn_benchmark]
                           [--max_train_steps MAX_TRAIN_STEPS]
                           [--max_train_epochs MAX_TRAIN_EPOCHS]
                           [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                           [--persistent_data_loader_workers] [--seed SEED]
                           [--gradient_checkpointing]
                           [--gradient_checkpointing_cpu_offload]
                           [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                           [--mixed_precision {no,fp16,bf16}]
                           [--logging_dir LOGGING_DIR]
                           [--log_with {tensorboard,wandb,all}]
                           [--log_prefix LOG_PREFIX]
                           [--log_tracker_name LOG_TRACKER_NAME]
                           [--wandb_run_name WANDB_RUN_NAME]
                           [--log_tracker_config LOG_TRACKER_CONFIG]
                           [--wandb_api_key WANDB_API_KEY] [--log_config]
                           [--ddp_timeout DDP_TIMEOUT]
                           [--ddp_gradient_as_bucket_view]
                           [--ddp_static_graph]
                           [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                           [--sample_at_first]
                           [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                           [--sample_prompts SAMPLE_PROMPTS]
                           [--optimizer_type OPTIMIZER_TYPE]
                           [--optimizer_args [OPTIMIZER_ARGS ...]]
                           [--learning_rate LEARNING_RATE]
                           [--max_grad_norm MAX_GRAD_NORM]
                           [--lr_scheduler LR_SCHEDULER]
                           [--lr_warmup_steps LR_WARMUP_STEPS]
                           [--lr_decay_steps LR_DECAY_STEPS]
                           [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                           [--lr_scheduler_power LR_SCHEDULER_POWER]
                           [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                           [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                           [--lr_scheduler_type LR_SCHEDULER_TYPE]
                           [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                           [--fp8_base]
                           [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                           [--dynamo_mode {default,reduce-overhead,max-autotune}]
                           [--dynamo_fullgraph] [--dynamo_dynamic]
                           [--blocks_to_swap BLOCKS_TO_SWAP]
                           [--use_pinned_memory_for_block_swap]
                           [--img_in_txt_in_offloading]
                           [--disable_numpy_memmap]
                           [--guidance_scale GUIDANCE_SCALE]
                           [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                           [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                           [--sigmoid_scale SIGMOID_SCALE]
                           [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                           [--logit_mean LOGIT_MEAN] [--logit_std LOGIT_STD]
                           [--mode_scale MODE_SCALE]
                           [--min_timestep MIN_TIMESTEP]
                           [--max_timestep MAX_TIMESTEP]
                           [--preserve_distribution_shape]
                           [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                           [--show_timesteps {image,console}] [--no_metadata]
                           [--network_weights NETWORK_WEIGHTS]
                           [--network_module NETWORK_MODULE]
                           [--network_dim NETWORK_DIM]
                           [--network_alpha NETWORK_ALPHA]
                           [--network_dropout NETWORK_DROPOUT]
                           [--network_args [NETWORK_ARGS ...]]
                           [--training_comment TRAINING_COMMENT]
                           [--dim_from_weights]
                           [--scale_weight_norms SCALE_WEIGHT_NORMS]
                           [--base_weights [BASE_WEIGHTS ...]]
                           [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                           [--output_dir OUTPUT_DIR]
                           [--output_name OUTPUT_NAME] [--resume RESUME]
                           [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                           [--save_every_n_steps SAVE_EVERY_N_STEPS]
                           [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                           [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                           [--save_last_n_steps SAVE_LAST_N_STEPS]
                           [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                           [--save_state] [--save_state_on_train_end]
                           [--metadata_title METADATA_TITLE]
                           [--metadata_author METADATA_AUTHOR]
                           [--metadata_description METADATA_DESCRIPTION]
                           [--metadata_license METADATA_LICENSE]
                           [--metadata_tags METADATA_TAGS]
                           [--metadata_reso METADATA_RESO]
                           [--metadata_arch METADATA_ARCH]
                           [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                           [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                           [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                           [--huggingface_token HUGGINGFACE_TOKEN]
                           [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                           [--save_state_to_huggingface]
                           [--resume_from_huggingface] [--async_upload]
                           [--dit DIT] [--vae VAE] [--vae_dtype VAE_DTYPE]
                           [--fp8_scaled] [--text_encoder TEXT_ENCODER]
                           [--fp8_vl] [--num_layers NUM_LAYERS]
                           [--remove_first_image_from_target] [--edit]
                           [--edit_plus] [--model_version MODEL_VERSION]
                           [--full_bf16] [--fused_backward_pass]
                           [--mem_eff_save]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --text_encoder TEXT_ENCODER
                        text encoder (Qwen2.5-VL) checkpoint path
  --fp8_vl              use fp8 for Text Encoder model
  --num_layers NUM_LAYERS
                        Number of layers in the DiT model, default is None
                        (60)
  --remove_first_image_from_target
                        Remove the first image from the target images for
                        layered model. / レイヤードモデルでターゲット画像から最初の画像を削除する。
  --edit                Enable Qwen-Image-Edit original, recommend
                        `--model_version edit` instead
  --edit_plus           Enable Qwen-Image-Edit-2509 (plus), recommend
                        `--model_version edit-2509` instead
  --model_version MODEL_VERSION
                        training for Qwen-Image model version, e.g.,
                        'original', 'layered', 'edit', 'edit-2509',
                        'edit-2511' etc.
  --full_bf16           Enable full bfloat16 training for Qwen-Image
  --fused_backward_pass
                        Use fused backward pass for Adafactor optimizer
  --mem_eff_save        Enable memory efficient saving (saving states requires
                        use normal saving, so it takes same amount of memory
                        even with this option enabled)

============================================================
### qwen_image_generate_image.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: qwen_image_generate_image.py [-h] [--dit DIT] [--num_layers NUM_LAYERS]
                                    [--disable_numpy_memmap] [--edit]
                                    [--edit_plus]
                                    [--model_version MODEL_VERSION]
                                    [--vae VAE] [--vae_enable_tiling]
                                    --text_encoder TEXT_ENCODER
                                    [--lora_weight [LORA_WEIGHT ...]]
                                    [--lora_multiplier [LORA_MULTIPLIER ...]]
                                    [--include_patterns [INCLUDE_PATTERNS ...]]
                                    [--exclude_patterns [EXCLUDE_PATTERNS ...]]
                                    [--save_merged_model SAVE_MERGED_MODEL]
                                    [--guidance_scale GUIDANCE_SCALE]
                                    [--prompt PROMPT]
                                    [--negative_prompt NEGATIVE_PROMPT]
                                    [--automatic_prompt_lang_for_layered {en,cn}]
                                    [--image_size IMAGE_SIZE IMAGE_SIZE]
                                    [--output_layers OUTPUT_LAYERS]
                                    [--control_image_path [CONTROL_IMAGE_PATH ...]]
                                    [--mask_path MASK_PATH]
                                    [--resize_control_to_image_size]
                                    [--resize_control_to_official_size]
                                    [--infer_steps INFER_STEPS] --save_path
                                    SAVE_PATH [--seed SEED]
                                    [--embedded_cfg_scale EMBEDDED_CFG_SCALE]
                                    [--flow_shift FLOW_SHIFT] [--fp8]
                                    [--fp8_scaled] [--text_encoder_cpu]
                                    [--device DEVICE]
                                    [--attn_mode {flash,torch,sageattn,xformers,sdpa}]
                                    [--blocks_to_swap BLOCKS_TO_SWAP]
                                    [--use_pinned_memory_for_block_swap]
                                    [--output_type {images,latent,latent_images}]
                                    [--no_metadata]
                                    [--latent_path [LATENT_PATH ...]]
                                    [--lycoris] [--append_original_name]
                                    [--compile]
                                    [--compile_backend COMPILE_BACKEND]
                                    [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                    [--compile_dynamic {true,false,auto}]
                                    [--compile_fullgraph]
                                    [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                    [--rcm_threshold RCM_THRESHOLD]
                                    [--rcm_relative_threshold]
                                    [--rcm_kernel_size RCM_KERNEL_SIZE]
                                    [--rcm_dilate_size RCM_DILATE_SIZE]
                                    [--rcm_debug_save] [--from_file FROM_FILE]
                                    [--interactive] [--bell]

Qwen-Image inference script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT directory or path
  --num_layers NUM_LAYERS
                        Number of layers in the DiT model, default is None
                        (60)
  --disable_numpy_memmap
                        Disable numpy memmap when loading safetensors. Default
                        is False.
  --edit                Enable Qwen-Image-Edit original, recommend
                        `--model_version edit` instead
  --edit_plus           Enable Qwen-Image-Edit-2509 (plus), recommend
                        `--model_version edit-2509` instead
  --model_version MODEL_VERSION
                        training for Qwen-Image model version, e.g.,
                        'original', 'layered', 'edit', 'edit-2509',
                        'edit-2511' etc.
  --vae VAE             VAE directory or path
  --vae_enable_tiling   Enable tiling for VAE decoding. Default is False.
  --text_encoder TEXT_ENCODER
                        Text Encoder 1 (Qwen2.5-VL) directory or path
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier
  --include_patterns [INCLUDE_PATTERNS ...]
                        LoRA module include patterns
  --exclude_patterns [EXCLUDE_PATTERNS ...]
                        LoRA module exclude patterns
  --save_merged_model SAVE_MERGED_MODEL
                        Save merged model to path. If specified, no inference
                        will be performed.
  --guidance_scale GUIDANCE_SCALE
                        Guidance scale for classifier free guidance. Default
                        is 4.0.
  --prompt PROMPT       prompt for generation
  --negative_prompt NEGATIVE_PROMPT
                        negative prompt for generation
  --automatic_prompt_lang_for_layered {en,cn}
                        Automatic prompt language for layered images. Enabled
                        only when prompt is not provided and model is layered.
  --image_size IMAGE_SIZE IMAGE_SIZE
                        image size, height and width
  --output_layers OUTPUT_LAYERS
                        number of output layers for layered model, default is
                        4
  --control_image_path [CONTROL_IMAGE_PATH ...]
                        path to control (reference) image(s) for Qwen-Image-
                        Edit, by default resized and cropped to 1M pixels
                        keeping aspect ratio.
  --mask_path MASK_PATH
                        path to mask image for Qwen-Image or Qwen-Image-Edit,
                        white for inpainting region
  --resize_control_to_image_size
                        resize control image to match image size
  --resize_control_to_official_size
                        resize control image to match official size (1M pixels
                        keeping aspect ratio)
  --infer_steps INFER_STEPS
                        number of inference steps, default is 25
  --save_path SAVE_PATH
                        path to save generated video
  --seed SEED           Seed for evaluation.
  --embedded_cfg_scale EMBEDDED_CFG_SCALE
                        Embeded CFG scale (distilled CFG Scale), default is
                        2.5
  --flow_shift FLOW_SHIFT
                        Shift factor for flow matching schedulers. Default is
                        None (use dynamic shifting based on image resolution).
  --fp8                 use fp8 for DiT model
  --fp8_scaled          use scaled fp8 for DiT, only for fp8
  --text_encoder_cpu    Inference on CPU for Text Encoder (Qwen2.5-VL)
  --device DEVICE       device to use for inference. If None, use CUDA if
                        available, otherwise use CPU
  --attn_mode {flash,torch,sageattn,xformers,sdpa}
                        attention mode
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows
  --output_type {images,latent,latent_images}
                        output type
  --no_metadata         do not save metadata
  --latent_path [LATENT_PATH ...]
                        path to latent for decode. no inference
  --lycoris             use lycoris for inference (not available)
  --append_original_name
                        append original base name when saving images when
                        editing
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --rcm_threshold RCM_THRESHOLD
                        RCM (Reference Consistency Mask) threshold, default is
                        None (disabled). Lower values mean larger inpainting
                        region. Typical values are 0.1 to 0.5 for relative
                        threshold, 0.01 to 0.1 for absolute threshold.
  --rcm_relative_threshold
                        If set, the RCM threshold is relative to the max of
                        diff_per_channel. Default is False.
  --rcm_kernel_size RCM_KERNEL_SIZE
                        RCM Gaussian kernel size, default is 3
  --rcm_dilate_size RCM_DILATE_SIZE
                        RCM mask dilation size, default is 0 (no dilation)
  --rcm_debug_save      If set, save the RCM mask for debugging. Cannot be
                        overridden by prompt line.
  --from_file FROM_FILE
                        Read prompts from a file
  --interactive         Interactive mode: read prompts from console
  --bell                Ring bell when done. For interactive mode, ring bell
                        on each iteration. For other modes, ring bell at the
                        end.

============================================================
### kandinsky5_cache_latents.py
============================================================
W0416 15:06:25.444000 58668 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
usage: kandinsky5_cache_latents.py [-h] --dataset_config DATASET_CONFIG
                                   [--vae VAE] [--vae_dtype VAE_DTYPE]
                                   [--device DEVICE] [--batch_size BATCH_SIZE]
                                   [--num_workers NUM_WORKERS]
                                   [--skip_existing] [--keep_cache]
                                   [--debug_mode {image,console,video}]
                                   [--console_width CONSOLE_WIDTH]
                                   [--console_back CONSOLE_BACK]
                                   [--console_num_images CONSOLE_NUM_IMAGES]
                                   [--disable_cudnn_backend] [--nabla_resize]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --vae VAE             path to vae checkpoint
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model, e.g.,
                        float16
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --debug_mode {image,console,video}
                        debug mode
  --console_width CONSOLE_WIDTH
                        debug mode: console width
  --console_back CONSOLE_BACK
                        debug mode: console background color, one of
                        ascii_magic.Back
  --console_num_images CONSOLE_NUM_IMAGES
                        debug mode: not interactive, number of images to show
                        for each dataset
  --disable_cudnn_backend
                        Disable CUDNN PyTorch backend. May be useful for AMD
                        GPUs.
  --nabla_resize        Resize inputs to the next multiple of 128 for NABLA-
                        compatible latents (H/W divisible by 16 after VAE).

============================================================
### kandinsky5_cache_text_encoder_outputs.py
============================================================
usage: kandinsky5_cache_text_encoder_outputs.py [-h] --dataset_config
                                                DATASET_CONFIG
                                                [--device DEVICE]
                                                [--batch_size BATCH_SIZE]
                                                [--num_workers NUM_WORKERS]
                                                [--skip_existing]
                                                [--keep_cache]
                                                --text_encoder_qwen
                                                TEXT_ENCODER_QWEN
                                                --text_encoder_clip
                                                TEXT_ENCODER_CLIP
                                                [--qwen_max_length QWEN_MAX_LENGTH]
                                                [--clip_max_length CLIP_MAX_LENGTH]
                                                [--quantized_qwen]

options:
  -h, --help            show this help message and exit
  --dataset_config DATASET_CONFIG
                        path to dataset config .toml file
  --device DEVICE       device to use, default is cuda if available
  --batch_size BATCH_SIZE
                        batch size, override dataset config if dataset batch
                        size > this
  --num_workers NUM_WORKERS
                        number of workers for dataset. default is cpu count-1
  --skip_existing       skip existing cache files
  --keep_cache          keep cache files not in dataset
  --text_encoder_qwen TEXT_ENCODER_QWEN
                        Qwen2.5-VL checkpoint path
  --text_encoder_clip TEXT_ENCODER_CLIP
                        CLIP text encoder checkpoint path
  --qwen_max_length QWEN_MAX_LENGTH
                        Max length for Qwen tokenizer
  --clip_max_length CLIP_MAX_LENGTH
                        Max length for CLIP tokenizer
  --quantized_qwen      Load Qwen text encoder in 4bit mode

============================================================
### kandinsky5_train_network.py
============================================================
W0416 15:06:35.892000 48948 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
Trying to import sageattention
Failed to import sageattention
usage: kandinsky5_train_network.py [-h] [--config_file CONFIG_FILE]
                                   [--dataset_config DATASET_CONFIG] [--sdpa]
                                   [--flash_attn] [--sage_attn] [--xformers]
                                   [--flash3] [--split_attn] [--compile]
                                   [--compile_backend COMPILE_BACKEND]
                                   [--compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}]
                                   [--compile_dynamic {true,false,auto}]
                                   [--compile_fullgraph]
                                   [--compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT]
                                   [--cuda_allow_tf32]
                                   [--cuda_cudnn_benchmark]
                                   [--max_train_steps MAX_TRAIN_STEPS]
                                   [--max_train_epochs MAX_TRAIN_EPOCHS]
                                   [--max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS]
                                   [--persistent_data_loader_workers]
                                   [--seed SEED] [--gradient_checkpointing]
                                   [--gradient_checkpointing_cpu_offload]
                                   [--gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS]
                                   [--mixed_precision {no,fp16,bf16}]
                                   [--logging_dir LOGGING_DIR]
                                   [--log_with {tensorboard,wandb,all}]
                                   [--log_prefix LOG_PREFIX]
                                   [--log_tracker_name LOG_TRACKER_NAME]
                                   [--wandb_run_name WANDB_RUN_NAME]
                                   [--log_tracker_config LOG_TRACKER_CONFIG]
                                   [--wandb_api_key WANDB_API_KEY]
                                   [--log_config] [--ddp_timeout DDP_TIMEOUT]
                                   [--ddp_gradient_as_bucket_view]
                                   [--ddp_static_graph]
                                   [--sample_every_n_steps SAMPLE_EVERY_N_STEPS]
                                   [--sample_at_first]
                                   [--sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS]
                                   [--sample_prompts SAMPLE_PROMPTS]
                                   [--optimizer_type OPTIMIZER_TYPE]
                                   [--optimizer_args [OPTIMIZER_ARGS ...]]
                                   [--learning_rate LEARNING_RATE]
                                   [--max_grad_norm MAX_GRAD_NORM]
                                   [--lr_scheduler LR_SCHEDULER]
                                   [--lr_warmup_steps LR_WARMUP_STEPS]
                                   [--lr_decay_steps LR_DECAY_STEPS]
                                   [--lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES]
                                   [--lr_scheduler_power LR_SCHEDULER_POWER]
                                   [--lr_scheduler_timescale LR_SCHEDULER_TIMESCALE]
                                   [--lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO]
                                   [--lr_scheduler_type LR_SCHEDULER_TYPE]
                                   [--lr_scheduler_args [LR_SCHEDULER_ARGS ...]]
                                   [--fp8_base]
                                   [--dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}]
                                   [--dynamo_mode {default,reduce-overhead,max-autotune}]
                                   [--dynamo_fullgraph] [--dynamo_dynamic]
                                   [--blocks_to_swap BLOCKS_TO_SWAP]
                                   [--use_pinned_memory_for_block_swap]
                                   [--img_in_txt_in_offloading]
                                   [--disable_numpy_memmap]
                                   [--guidance_scale GUIDANCE_SCALE]
                                   [--timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}]
                                   [--discrete_flow_shift DISCRETE_FLOW_SHIFT]
                                   [--sigmoid_scale SIGMOID_SCALE]
                                   [--weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}]
                                   [--logit_mean LOGIT_MEAN]
                                   [--logit_std LOGIT_STD]
                                   [--mode_scale MODE_SCALE]
                                   [--min_timestep MIN_TIMESTEP]
                                   [--max_timestep MAX_TIMESTEP]
                                   [--preserve_distribution_shape]
                                   [--num_timestep_buckets NUM_TIMESTEP_BUCKETS]
                                   [--show_timesteps {image,console}]
                                   [--no_metadata]
                                   [--network_weights NETWORK_WEIGHTS]
                                   [--network_module NETWORK_MODULE]
                                   [--network_dim NETWORK_DIM]
                                   [--network_alpha NETWORK_ALPHA]
                                   [--network_dropout NETWORK_DROPOUT]
                                   [--network_args [NETWORK_ARGS ...]]
                                   [--training_comment TRAINING_COMMENT]
                                   [--dim_from_weights]
                                   [--scale_weight_norms SCALE_WEIGHT_NORMS]
                                   [--base_weights [BASE_WEIGHTS ...]]
                                   [--base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]]
                                   [--output_dir OUTPUT_DIR]
                                   [--output_name OUTPUT_NAME]
                                   [--resume RESUME]
                                   [--save_every_n_epochs SAVE_EVERY_N_EPOCHS]
                                   [--save_every_n_steps SAVE_EVERY_N_STEPS]
                                   [--save_last_n_epochs SAVE_LAST_N_EPOCHS]
                                   [--save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE]
                                   [--save_last_n_steps SAVE_LAST_N_STEPS]
                                   [--save_last_n_steps_state SAVE_LAST_N_STEPS_STATE]
                                   [--save_state] [--save_state_on_train_end]
                                   [--metadata_title METADATA_TITLE]
                                   [--metadata_author METADATA_AUTHOR]
                                   [--metadata_description METADATA_DESCRIPTION]
                                   [--metadata_license METADATA_LICENSE]
                                   [--metadata_tags METADATA_TAGS]
                                   [--metadata_reso METADATA_RESO]
                                   [--metadata_arch METADATA_ARCH]
                                   [--huggingface_repo_id HUGGINGFACE_REPO_ID]
                                   [--huggingface_repo_type HUGGINGFACE_REPO_TYPE]
                                   [--huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO]
                                   [--huggingface_token HUGGINGFACE_TOKEN]
                                   [--huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY]
                                   [--save_state_to_huggingface]
                                   [--resume_from_huggingface]
                                   [--async_upload] [--dit DIT] [--vae VAE]
                                   [--vae_dtype VAE_DTYPE] --task TASK
                                   [--override_dit OVERRIDE_DIT]
                                   [--fp8_scaled]
                                   [--text_encoder_qwen TEXT_ENCODER_QWEN]
                                   [--text_encoder_clip TEXT_ENCODER_CLIP]
                                   [--offload_dit_during_sampling]
                                   [--no_vae_load]
                                   [--scheduler_scale SCHEDULER_SCALE]
                                   [--i2v_mode {first,first_last}]
                                   [--force_nabla_attention]
                                   [--nabla_P NABLA_P] [--nabla_wT NABLA_WT]
                                   [--nabla_wH NABLA_WH] [--nabla_wW NABLA_WW]
                                   [--nabla_method NABLA_METHOD]
                                   [--nabla_add_sta] [--no_nabla_add_sta]

options:
  -h, --help            show this help message and exit
  --config_file CONFIG_FILE
                        using .toml instead of args to pass hyperparameter /
                        ハイパーパラメータを引数ではなく.tomlファイルで渡す
  --dataset_config DATASET_CONFIG
                        config file for dataset / データセットの設定ファイル
  --sdpa                use sdpa for CrossAttention (requires PyTorch 2.0) /
                        CrossAttentionにsdpaを使う（PyTorch 2.0が必要）
  --flash_attn          use FlashAttention for CrossAttention, requires
                        FlashAttention /
                        CrossAttentionにFlashAttentionを使う、FlashAttentionが必要
  --sage_attn           use SageAttention. requires SageAttention /
                        SageAttentionを使う。SageAttentionが必要
  --xformers            use xformers for CrossAttention, requires xformers /
                        CrossAttentionにxformersを使う、xformersが必要
  --flash3              use FlashAttention 3 for CrossAttention, requires
                        FlashAttention 3, HunyuanVideo does not support this
                        yet / CrossAttentionにFlashAttention
                        3を使う、FlashAttention 3が必要。HunyuanVideoは未対応。
  --split_attn          use split attention for attention calculation (split
                        batch size=1, affects memory usage and speed) /
                        attentionを分割して計算する（バッチサイズ=1に分割、メモリ使用量と速度に影響）
  --compile             Enable torch.compile (requires Triton) /
                        torch.compileを有効にする（Tritonが必要）
  --compile_backend COMPILE_BACKEND
                        torch.compile backend (default: inductor) /
                        torch.compileのバックエンド（デフォルト: inductor）
  --compile_mode {default,reduce-overhead,max-autotune,max-autotune-no-cudagraphs}
                        torch.compile mode (default: default) /
                        torch.compileのモード（デフォルト: default）
  --compile_dynamic {true,false,auto}
                        Dynamic shapes mode for torch.compile (default: None,
                        same as auto) / torch.compileの動的形状モード（デフォルト:
                        None、autoと同じ動作）
  --compile_fullgraph   Enable fullgraph mode in torch.compile /
                        torch.compileでフルグラフモードを有効にする
  --compile_cache_size_limit COMPILE_CACHE_SIZE_LIMIT
                        Set torch._dynamo.config.cache_size_limit (default:
                        PyTorch default, typically 8-32) /
                        torch._dynamo.config.cache_size_limitを設定（デフォルト:
                        PyTorchのデフォルト、通常8-32）
  --cuda_allow_tf32     Allow TF32 on Ampere or higher GPUs /
                        Ampere以降のGPUでTF32を許可する
  --cuda_cudnn_benchmark
                        Enable cudnn benchmark for possibly faster training /
                        cudnnのベンチマークを有効にして学習の高速化を図る
  --max_train_steps MAX_TRAIN_STEPS
                        training steps / 学習ステップ数
  --max_train_epochs MAX_TRAIN_EPOCHS
                        training epochs (overrides max_train_steps) /
                        学習エポック数（max_train_stepsを上書きします）
  --max_data_loader_n_workers MAX_DATA_LOADER_N_WORKERS
                        max num workers for DataLoader (lower is less main RAM
                        usage, faster epoch start and slower data loading) / D
                        ataLoaderの最大プロセス数（小さい値ではメインメモリの使用量が減りエポック間の待ち時間が減りますが、
                        データ読み込みは遅くなります）
  --persistent_data_loader_workers
                        persistent DataLoader workers (useful for reduce time
                        gap between epoch, but may use more memory) /
                        DataLoader のワーカーを持続させる
                        (エポック間の時間差を少なくするのに有効だが、より多くのメモリを消費する可能性がある)
  --seed SEED           random seed for training / 学習時の乱数のseed
  --gradient_checkpointing
                        enable gradient checkpointing / gradient
                        checkpointingを有効にする
  --gradient_checkpointing_cpu_offload
                        enable CPU offloading of activation for gradient
                        checkpointing / gradient
                        checkpointing時に活性化のCPUオフロードを有効にする
  --gradient_accumulation_steps GRADIENT_ACCUMULATION_STEPS
                        Number of updates steps to accumulate before
                        performing a backward/update pass /
                        学習時に逆伝播をする前に勾配を合計するステップ数
  --mixed_precision {no,fp16,bf16}
                        use mixed precision / 混合精度を使う場合、その精度
  --logging_dir LOGGING_DIR
                        enable logging and output TensorBoard log to this
                        directory / ログ出力を有効にしてこのディレクトリにTensorBoard用のログを出力する
  --log_with {tensorboard,wandb,all}
                        what logging tool(s) to use (if 'all', TensorBoard and
                        WandB are both used) / ログ出力に使用するツール
                        (allを指定するとTensorBoardとWandBの両方が使用される)
  --log_prefix LOG_PREFIX
                        add prefix for each log directory /
                        ログディレクトリ名の先頭に追加する文字列
  --log_tracker_name LOG_TRACKER_NAME
                        name of tracker to use for logging, default is script-
                        specific default name /
                        ログ出力に使用するtrackerの名前、省略時はスクリプトごとのデフォルト名
  --wandb_run_name WANDB_RUN_NAME
                        The name of the specific wandb session / wandb
                        ログに表示される特定の実行の名前
  --log_tracker_config LOG_TRACKER_CONFIG
                        path to tracker config file to use for logging /
                        ログ出力に使用するtrackerの設定ファイルのパス
  --wandb_api_key WANDB_API_KEY
                        specify WandB API key to log in before starting
                        training (optional). / WandB
                        APIキーを指定して学習開始前にログインする（オプション）
  --log_config          log training configuration / 学習設定をログに出力する
  --ddp_timeout DDP_TIMEOUT
                        DDP timeout (min, None for default of accelerate) /
                        DDPのタイムアウト（分、Noneでaccelerateのデフォルト）
  --ddp_gradient_as_bucket_view
                        enable gradient_as_bucket_view for DDP /
                        DDPでgradient_as_bucket_viewを有効にする
  --ddp_static_graph    enable static_graph for DDP / DDPでstatic_graphを有効にする
  --sample_every_n_steps SAMPLE_EVERY_N_STEPS
                        generate sample images every N steps /
                        学習中のモデルで指定ステップごとにサンプル出力する
  --sample_at_first     generate sample images before training / 学習前にサンプル出力する
  --sample_every_n_epochs SAMPLE_EVERY_N_EPOCHS
                        generate sample images every N epochs (overwrites
                        n_steps) / 学習中のモデルで指定エポックごとにサンプル出力する（ステップ数指定を上書きします）
  --sample_prompts SAMPLE_PROMPTS
                        file for prompts to generate sample images /
                        学習中モデルのサンプル出力用プロンプトのファイル
  --optimizer_type OPTIMIZER_TYPE
                        Optimizer to use / オプティマイザの種類: AdamW (default),
                        AdamW8bit, AdaFactor. Also, you can use any optimizer
                        by specifying the full path to the class, like
                        'torch.optim.AdamW', 'bitsandbytes.optim.AdEMAMix8bit'
                        or 'bitsandbytes.optim.PagedAdEMAMix8bit' etc. /
  --optimizer_args [OPTIMIZER_ARGS ...]
                        additional arguments for optimizer (like
                        "weight_decay=0.01 betas=0.9,0.999 ...") /
                        オプティマイザの追加引数（例： "weight_decay=0.01 betas=0.9,0.999
                        ..."）
  --learning_rate LEARNING_RATE
                        learning rate / 学習率
  --max_grad_norm MAX_GRAD_NORM
                        Max gradient norm, 0 for no clipping /
                        勾配正規化の最大norm、0でclippingを行わない
  --lr_scheduler LR_SCHEDULER
                        scheduler to use for learning rate / 学習率のスケジューラ:
                        linear, cosine, cosine_with_restarts, polynomial,
                        constant (default), constant_with_warmup, adafactor,
                        rex
  --lr_warmup_steps LR_WARMUP_STEPS
                        Int number of steps for the warmup in the lr scheduler
                        (default is 0) or float with ratio of train steps / 学習
                        率のスケジューラをウォームアップするステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_decay_steps LR_DECAY_STEPS
                        Int number of steps for the decay in the lr scheduler
                        (default is 0) or float (<1) with ratio of train steps
                        / 学習率のスケジューラを減衰させるステップ数（デフォルト0）、または学習ステップの比率（1未満のfloat
                        値の場合）
  --lr_scheduler_num_cycles LR_SCHEDULER_NUM_CYCLES
                        Number of restarts for cosine scheduler with restarts
                        / cosine with restartsスケジューラでのリスタート回数
  --lr_scheduler_power LR_SCHEDULER_POWER
                        Polynomial power for polynomial scheduler /
                        polynomialスケジューラでのpolynomial power
  --lr_scheduler_timescale LR_SCHEDULER_TIMESCALE
                        Inverse sqrt timescale for inverse sqrt
                        scheduler,defaults to `num_warmup_steps` /
                        逆平方根スケジューラのタイムスケール、デフォルトは`num_warmup_steps`
  --lr_scheduler_min_lr_ratio LR_SCHEDULER_MIN_LR_RATIO
                        The minimum learning rate as a ratio of the initial
                        learning rate for cosine with min lr scheduler, warmup
                        decay scheduler and rex scheduler /
                        初期学習率の比率としての最小学習率を指定する、cosine with min lr
                        スケジューラ、warmup decay スケジューラ、rex スケジューラ で有効
  --lr_scheduler_type LR_SCHEDULER_TYPE
                        custom scheduler module / 使用するスケジューラ
  --lr_scheduler_args [LR_SCHEDULER_ARGS ...]
                        additional arguments for scheduler (like "T_max=100")
                        / スケジューラの追加引数（例： "T_max100"）
  --fp8_base            use fp8 for base model / base modelにfp8を使う
  --dynamo_backend {NO,EAGER,AOT_EAGER,INDUCTOR,AOT_TS_NVFUSER,NVPRIMS_NVFUSER,CUDAGRAPHS,OFI,FX2TRT,ONNXRT,TENSORRT,AOT_TORCHXLA_TRACE_ONCE,TORCHXLA_TRACE_ONCE,IPEX,TVM,HPU_BACKEND}
                        dynamo backend type (default is None) /
                        dynamoのbackendの種類（デフォルトは None）
  --dynamo_mode {default,reduce-overhead,max-autotune}
                        dynamo mode (default is default) / dynamoのモード（デフォルトは
                        default）
  --dynamo_fullgraph    use fullgraph mode for dynamo / dynamoのfullgraphモードを使う
  --dynamo_dynamic      use dynamic mode for dynamo / dynamoのdynamicモードを使う
  --blocks_to_swap BLOCKS_TO_SWAP
                        number of blocks to swap in the model, max XXX /
                        モデル内のブロックの数、最大XXX
  --use_pinned_memory_for_block_swap
                        use pinned memory for block swapping, which may speed
                        up data transfer between CPU and GPU but uses more
                        shared GPU memory on Windows / ブロックスワッピングにピン留めメモリを使用する
                        。これによりCPUとGPU間のデータ転送が高速化される可能性があるが、Windowsではより多くの共有GPU
                        メモリを使用する。
  --img_in_txt_in_offloading
                        offload img_in and txt_in to cpu /
                        img_inとtxt_inをCPUにオフロードする
  --disable_numpy_memmap
                        Disable numpy memory mapping for model loading. Only
                        for Wan, FramePack, Qwen-Image and FLUX.2. Increases
                        RAM usage but speeds up model loading in some cases. /
                        モデル読み込み時のnumpyメモリマッピングを無効にします。Wan、FramePack、Qwen-Image
                        、FLUX.2で有効です。RAM使用量が増えますが、場合によってはモデルの読み込みが高速化されます。
  --guidance_scale GUIDANCE_SCALE
                        Embeded classifier free guidance scale (HunyuanVideo
                        only).
  --timestep_sampling {sigma,uniform,sigmoid,shift,flux_shift,flux2_shift,qwen_shift,logsnr,qinglong_flux,qinglong_qwen}
                        Method to sample timesteps: sigma-based, uniform
                        random, sigmoid of random normal, shift of sigmoid and
                        flux shift. / タイムステップをサンプリングする方法：sigma、random
                        uniform、random normalのsigmoid、sigmoidのシフト、flux shift。
  --discrete_flow_shift DISCRETE_FLOW_SHIFT
                        Discrete flow shift for the Euler Discrete Scheduler,
                        default is 1.0. / Euler Discrete
                        Schedulerの離散フローシフト、デフォルトは1.0。
  --sigmoid_scale SIGMOID_SCALE
                        Scale factor for sigmoid timestep sampling (only used
                        when timestep-sampling is "sigmoid" or "shift"). /
                        sigmoidタイムステップサンプリングの倍率（timestep-
                        samplingが"sigmoid"または"shift"の場合のみ有効）。
  --weighting_scheme {logit_normal,mode,cosmap,sigma_sqrt,none}
                        weighting scheme for timestep distribution. Default is
                        none / タイムステップ分布の重み付けスキーム、デフォルトはnone
  --logit_mean LOGIT_MEAN
                        mean to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合の平均
  --logit_std LOGIT_STD
                        std to use when using the `'logit_normal'` weighting
                        scheme / `'logit_normal'`重み付けスキームを使用する場合のstd
  --mode_scale MODE_SCALE
                        Scale of mode weighting scheme. Only effective when
                        using the `'mode'` as the `weighting_scheme` /
                        モード重み付けスキームのスケール
  --min_timestep MIN_TIMESTEP
                        set minimum time step for training (0~999, default is
                        0) / 学習時のtime stepの最小値を設定する（0~999で指定、省略時はデフォルト値(0)）
  --max_timestep MAX_TIMESTEP
                        set maximum time step for training (1~1000, default is
                        1000) / 学習時のtime
                        stepの最大値を設定する（1~1000で指定、省略時はデフォルト値(1000)）
  --preserve_distribution_shape
                        If specified, constrains timestep sampling to
                        [min_timestep, max_timestep] using rejection sampling,
                        preserving the original distribution shape. By
                        default, the [0, 1] range is scaled, which distorts
                        the distribution. Only effective when
                        `timestep_sampling` is not 'sigma'. / 指定すると、タイムステップのサン
                        プリングを[最小タイムステップ、最大タイムステップ]に制約し、元の分布形状を保持します。デフォルトでは、[0
                        , 1]の範囲がスケーリングされ、分布が歪むことがあります。timestep_samplingがsigma以
                        外で有効です。
  --num_timestep_buckets NUM_TIMESTEP_BUCKETS
                        Number of buckets for timestep sampling. Default is
                        None, which disables bucketing. Set to 2 or more to
                        enable stratified sampling. This forces timesteps to
                        be sampled uniformly from the [0, 1] range, which can
                        improve training stability, especially for small
                        datasets. / timestepサンプリングのバケット数。デフォルトはNoneで、バケット化を無効に
                        します。2以上に設定すると、層化抽出が有効になり、タイムステップが[0, 1]の範囲から均等にサンプリングさ
                        れるようになります。これは、特に小規模なデータセットでの学習の安定性向上が期待できます。
  --show_timesteps {image,console}
                        show timesteps in image or console, and return to
                        console / タイムステップを画像またはコンソールに表示し、コンソールに戻る
  --no_metadata         do not save metadata in output model /
                        メタデータを出力先モデルに保存しない
  --network_weights NETWORK_WEIGHTS
                        pretrained weights for network / 学習するネットワークの初期重み
  --network_module NETWORK_MODULE
                        network module to train / 学習対象のネットワークのモジュール
  --network_dim NETWORK_DIM
                        network dimensions (depends on each network) /
                        モジュールの次元数（ネットワークにより定義は異なります）
  --network_alpha NETWORK_ALPHA
                        alpha for LoRA weight scaling, default 1 (same as
                        network_dim for same behavior as old version) / LoRaの重
                        み調整のalpha値、デフォルト1（旧バージョンと同じ動作をするにはnetwork_dimと同じ値を指定）
  --network_dropout NETWORK_DROPOUT
                        Drops neurons out of training every step (0 or None is
                        default behavior (no dropout), 1 would drop all
                        neurons) / 訓練時に毎ステップでニューロンをdropする（0またはNoneはdropoutなし、1
                        は全ニューロンをdropout）
  --network_args [NETWORK_ARGS ...]
                        additional arguments for network (key=value) /
                        ネットワークへの追加の引数
  --training_comment TRAINING_COMMENT
                        arbitrary comment string stored in metadata /
                        メタデータに記録する任意のコメント文字列
  --dim_from_weights    automatically determine dim (rank) from
                        network_weights / dim
                        (rank)をnetwork_weightsで指定した重みから自動で決定する
  --scale_weight_norms SCALE_WEIGHT_NORMS
                        Scale the weight of each key pair to help prevent
                        overtraing via exploding gradients. (1 is a good
                        starting point) / 重みの値をスケーリングして勾配爆発を防ぐ（1が初期値としては適当）
  --base_weights [BASE_WEIGHTS ...]
                        network weights to merge into the model before
                        training / 学習前にあらかじめモデルにマージするnetworkの重みファイル
  --base_weights_multiplier [BASE_WEIGHTS_MULTIPLIER ...]
                        multiplier for network weights to merge into the model
                        before training / 学習前にあらかじめモデルにマージするnetworkの重みの倍率
  --output_dir OUTPUT_DIR
                        directory to output trained model / 学習後のモデル出力先ディレクトリ
  --output_name OUTPUT_NAME
                        base name of trained model file / 学習後のモデルの拡張子を除くファイル名
  --resume RESUME       saved state to resume training / 学習再開するモデルのstate
  --save_every_n_epochs SAVE_EVERY_N_EPOCHS
                        save checkpoint every N epochs / 学習中のモデルを指定エポックごとに保存する
  --save_every_n_steps SAVE_EVERY_N_STEPS
                        save checkpoint every N steps / 学習中のモデルを指定ステップごとに保存する
  --save_last_n_epochs SAVE_LAST_N_EPOCHS
                        save last N checkpoints when saving every N epochs
                        (remove older checkpoints) /
                        指定エポックごとにモデルを保存するとき最大Nエポック保存する（古いチェックポイントは削除する）
  --save_last_n_epochs_state SAVE_LAST_N_EPOCHS_STATE
                        save last N checkpoints of state (overrides the value
                        of --save_last_n_epochs)/
                        最大Nエポックstateを保存する（--save_last_n_epochsの指定を上書きする）
  --save_last_n_steps SAVE_LAST_N_STEPS
                        save checkpoints until N steps elapsed (remove older
                        checkpoints if N steps elapsed) / 指定ステップごとにモデルを保存するとき、
                        このステップ数経過するまで保存する（このステップ数経過したら削除する）
  --save_last_n_steps_state SAVE_LAST_N_STEPS_STATE
                        save states until N steps elapsed (remove older states
                        if N steps elapsed, overrides --save_last_n_steps) / 指
                        定ステップごとにstateを保存するとき、このステップ数経過するまで保存する（このステップ数経過したら削除す
                        る。--save_last_n_stepsを上書きする）
  --save_state          save training state additionally (including optimizer
                        states etc.) when saving model /
                        optimizerなど学習状態も含めたstateをモデル保存時に追加で保存する
  --save_state_on_train_end
                        save training state (including optimizer states etc.)
                        on train end even if --save_state is not specified /
                        --save_stateが未指定時にもoptimizerなど学習状態も含めたstateを学習終了時に保存する
  --metadata_title METADATA_TITLE
                        title for model metadata (default is output_name) /
                        メタデータに書き込まれるモデルタイトル、省略時はoutput_name
  --metadata_author METADATA_AUTHOR
                        author name for model metadata / メタデータに書き込まれるモデル作者名
  --metadata_description METADATA_DESCRIPTION
                        description for model metadata / メタデータに書き込まれるモデル説明
  --metadata_license METADATA_LICENSE
                        license for model metadata / メタデータに書き込まれるモデルライセンス
  --metadata_tags METADATA_TAGS
                        tags for model metadata, separated by comma /
                        メタデータに書き込まれるモデルタグ、カンマ区切り
  --metadata_reso METADATA_RESO
                        resolution for model metadata (e.g., `1024,1024`) /
                        メタデータに書き込まれるモデル解像度（例: `1024,1024`）
  --metadata_arch METADATA_ARCH
                        architecture for model metadata /
                        メタデータに書き込まれるモデルアーキテクチャ
  --huggingface_repo_id HUGGINGFACE_REPO_ID
                        huggingface repo name to upload /
                        huggingfaceにアップロードするリポジトリ名
  --huggingface_repo_type HUGGINGFACE_REPO_TYPE
                        huggingface repo type to upload /
                        huggingfaceにアップロードするリポジトリの種類
  --huggingface_path_in_repo HUGGINGFACE_PATH_IN_REPO
                        huggingface model path to upload files /
                        huggingfaceにアップロードするファイルのパス
  --huggingface_token HUGGINGFACE_TOKEN
                        huggingface token / huggingfaceのトークン
  --huggingface_repo_visibility HUGGINGFACE_REPO_VISIBILITY
                        huggingface repository visibility ('public' for
                        public, 'private' or None for private) / huggingfaceにア
                        ップロードするリポジトリの公開設定（'public'で公開、'private'またはNoneで非公開）
  --save_state_to_huggingface
                        save state to huggingface / huggingfaceにstateを保存する
  --resume_from_huggingface
                        resume from huggingface (ex: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type}) /
                        huggingfaceから学習を再開する(例: --resume
                        {repo_id}/{path_in_repo}:{revision}:{repo_type})
  --async_upload        upload to huggingface asynchronously /
                        huggingfaceに非同期でアップロードする
  --dit DIT             DiT checkpoint path / DiTのチェックポイントのパス
  --vae VAE             VAE checkpoint path / VAEのチェックポイントのパス
  --vae_dtype VAE_DTYPE
                        data type for VAE, default depends on model
  --task TASK           Kandinsky5 task key (see configs.TASK_CONFIGS)
  --override_dit OVERRIDE_DIT
                        JSON dict to override DiT params
  --fp8_scaled          use scaled fp8 for DiT / DiTにスケーリングされたfp8を使う
  --text_encoder_qwen TEXT_ENCODER_QWEN
                        Override Qwen text encoder path
  --text_encoder_clip TEXT_ENCODER_CLIP
                        Override CLIP text encoder path
  --offload_dit_during_sampling
                        Offload DiT during sampling
  --no_vae_load         Skip loading VAE; use cached scaling factor only
  --scheduler_scale SCHEDULER_SCALE
                        Override scheduler scale (default from task config)
  --i2v_mode {first,first_last}
                        I2V conditioning mode: first frame only (default) or
                        first+last frame.
  --force_nabla_attention
                        Force nabla attention for training regardless of task
                        default
  --nabla_P NABLA_P     CDF threshold P for nabla attention (default 0.9)
  --nabla_wT NABLA_WT   Temporal STA window for nabla attention (default 11)
  --nabla_wH NABLA_WH   Height STA window for nabla attention (default 3)
  --nabla_wW NABLA_WW   Width STA window for nabla attention (default 3)
  --nabla_method NABLA_METHOD
                        Nabla map binarization method (default topcdf)
  --nabla_add_sta       Include STA prior when forcing nabla attention
                        (default: True)
  --no_nabla_add_sta    Disable STA prior when forcing nabla attention

============================================================
### kandinsky5_generate_video.py
============================================================
W0416 15:06:41.476000 49080 .venv\Lib\site-packages\torch\utils\flop_counter.py:29] triton not found; flop counting will not work for triton kernels
Trying to import sageattention
Failed to import sageattention
usage: kandinsky5_generate_video.py [-h]
                                    [--task {k5-lite-t2i-hd,k5-lite-i2i-hd,k5-lite-t2v-5s-sd,k5-lite-t2v-10s-sd,k5-lite-i2v-5s-sd,k5-pro-t2v-5s-sd,k5-pro-t2v-5s-hd,k5-pro-t2v-10s-sd,k5-pro-t2v-10s-hd,k5-pro-i2v-5s-sd,k5-pro-i2v-5s-hd,k5-lite-t2v-5s-distil-sd,k5-lite-t2v-10s-distil-sd,k5-lite-t2v-5s-nocfg-sd,k5-lite-t2v-10s-nocfg-sd,k5-lite-t2v-5s-pretrain-sd,k5-lite-t2v-10s-pretrain-sd}]
                                    --prompt PROMPT
                                    [--negative_prompt NEGATIVE_PROMPT]
                                    [--i IMAGE] [--image_last IMAGE_LAST]
                                    --output OUTPUT [--width WIDTH]
                                    [--height HEIGHT] [--frames FRAMES]
                                    [--steps STEPS] [--guidance GUIDANCE]
                                    [--scheduler_scale SCHEDULER_SCALE]
                                    [--seed SEED] [--device DEVICE]
                                    [--dit DIT] [--vae VAE]
                                    [--text_encoder_qwen TEXT_ENCODER_QWEN]
                                    [--text_encoder_clip TEXT_ENCODER_CLIP]
                                    [--dtype {bfloat16,float16,float32}]
                                    [--blocks_to_swap BLOCKS_TO_SWAP]
                                    [--offload_dit_during_sampling]
                                    [--fp8_base] [--fp8_scaled] [--fp8_fast]
                                    [--disable_numpy_memmap] [--sdpa]
                                    [--flash_attn] [--flash3] [--sage_attn]
                                    [--xformers]
                                    [--lora_weight [LORA_WEIGHT ...]]
                                    [--lora_multiplier [LORA_MULTIPLIER ...]]

Kandinsky5 sampling (mirrors training sampler, no training)

options:
  -h, --help            show this help message and exit
  --task {k5-lite-t2i-hd,k5-lite-i2i-hd,k5-lite-t2v-5s-sd,k5-lite-t2v-10s-sd,k5-lite-i2v-5s-sd,k5-pro-t2v-5s-sd,k5-pro-t2v-5s-hd,k5-pro-t2v-10s-sd,k5-pro-t2v-10s-hd,k5-pro-i2v-5s-sd,k5-pro-i2v-5s-hd,k5-lite-t2v-5s-distil-sd,k5-lite-t2v-10s-distil-sd,k5-lite-t2v-5s-nocfg-sd,k5-lite-t2v-10s-nocfg-sd,k5-lite-t2v-5s-pretrain-sd,k5-lite-t2v-10s-pretrain-sd}
  --prompt PROMPT
  --negative_prompt NEGATIVE_PROMPT
  --i IMAGE, --image IMAGE
                        Init image path for i2v-style seeding (first frame)
  --image_last IMAGE_LAST
                        Optional last-frame image path for i2v first_last
                        conditioning
  --output OUTPUT
  --width WIDTH
  --height HEIGHT
  --frames FRAMES
  --steps STEPS
  --guidance GUIDANCE
  --scheduler_scale SCHEDULER_SCALE
  --seed SEED
  --device DEVICE
  --dit DIT
  --vae VAE
  --text_encoder_qwen TEXT_ENCODER_QWEN
  --text_encoder_clip TEXT_ENCODER_CLIP
  --dtype {bfloat16,float16,float32}
  --blocks_to_swap BLOCKS_TO_SWAP
  --offload_dit_during_sampling
  --fp8_base
  --fp8_scaled
  --fp8_fast
  --disable_numpy_memmap
  --sdpa                use SDPA for visual attention
  --flash_attn          use FlashAttention 2 for visual attention
  --flash3              use FlashAttention 3 for visual attention
  --sage_attn           use SageAttention for visual attention
  --xformers            use xformers for visual attention
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path(s) to merge for inference
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier(s), align with lora_weight order

============================================================
### convert_lora.py
============================================================
usage: convert_lora.py [-h] --input INPUT --output OUTPUT --target
                       {other,default} [--diffusers_prefix DIFFUSERS_PREFIX]

Convert LoRA/LoHa/LoKr weights between default and other formats

options:
  -h, --help            show this help message and exit
  --input INPUT         input model file
  --output OUTPUT       output model file
  --target {other,default}
                        target format
  --diffusers_prefix DIFFUSERS_PREFIX
                        prefix for Diffusers weights, default is None (use
                        `diffusion_model`)

============================================================
### merge_lora.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: merge_lora.py [-h] --dit DIT [--dit_in_channels DIT_IN_CHANNELS]
                     [--lora_weight [LORA_WEIGHT ...]]
                     [--lora_multiplier [LORA_MULTIPLIER ...]]
                     --save_merged_model SAVE_MERGED_MODEL [--device DEVICE]

HunyuanVideo model merger script

options:
  -h, --help            show this help message and exit
  --dit DIT             DiT checkpoint path or directory
  --dit_in_channels DIT_IN_CHANNELS
                        input channels for DiT, default is 16, skyreels I2V is
                        32
  --lora_weight [LORA_WEIGHT ...]
                        LoRA weight path
  --lora_multiplier [LORA_MULTIPLIER ...]
                        LoRA multiplier (can specify multiple values)
  --save_merged_model SAVE_MERGED_MODEL
                        Path to save the merged model
  --device DEVICE       Device to use for merging

============================================================
### lora_post_hoc_ema.py
============================================================
usage: lora_post_hoc_ema.py [-h] [--no_sort] [--beta BETA] [--beta2 BETA2]
                            [--sigma_rel SIGMA_REL] --output_file OUTPUT_FILE
                            path [path ...]

Merge LoRA weights with Post-Hoc EMA method.

positional arguments:
  path                  List of paths to the LoRA weight files.

options:
  -h, --help            show this help message and exit
  --no_sort             Do not sort the files by modification time.
  --beta BETA           Decay rate for merging weights.
  --beta2 BETA2         Decay rate for merging weights for linear
                        interpolation.
  --sigma_rel SIGMA_REL
                        Relative sigma for Power Function EMA, default is None
                        (linear interpolation).
  --output_file OUTPUT_FILE
                        Output file path for merged weights.

============================================================
### qwen_extract_lora.py
============================================================
usage: qwen_extract_lora.py [-h] --model_org MODEL_ORG --model_tuned
                            MODEL_TUNED --save_to SAVE_TO [--dim DIM]
                            [--device DEVICE]
                            [--clamp_quantile CLAMP_QUANTILE]
                            [--mem_eff_safe_open]
                            [--save_precision {None,float,fp16,bf16}]
                            [--no_metadata]

options:
  -h, --help            show this help message and exit
  --model_org MODEL_ORG
  --model_tuned MODEL_TUNED
  --save_to SAVE_TO
  --dim DIM
  --device DEVICE
  --clamp_quantile CLAMP_QUANTILE
  --mem_eff_safe_open
  --save_precision {None,float,fp16,bf16}
  --no_metadata

============================================================
### caption_images_by_qwen_vl.py
============================================================
Trying to import sageattention
Failed to import sageattention
usage: caption_images_by_qwen_vl.py [-h] --image_dir IMAGE_DIR --model_path
                                    MODEL_PATH [--output_file OUTPUT_FILE]
                                    [--max_new_tokens MAX_NEW_TOKENS]
                                    [--prompt PROMPT] [--max_size MAX_SIZE]
                                    [--fp8_vl] [--output_format {jsonl,text}]

Generate captions for images using Qwen2.5-VL

options:
  -h, --help            show this help message and exit
  --image_dir IMAGE_DIR
                        Path to directory containing images
  --model_path MODEL_PATH
                        Path to Qwen2.5-VL model
  --output_file OUTPUT_FILE
                        Output JSONL file path (required for 'jsonl' format)
  --max_new_tokens MAX_NEW_TOKENS
                        Maximum number of new tokens to generate (default:
                        1024)
  --prompt PROMPT       Custom prompt for caption generation (supports \n for
                        newlines)
  --max_size MAX_SIZE   Maximum image size (default: 1280). The images are
                        resized to fit the total pixel area within (max_size x
                        max_size)
  --fp8_vl              Load Qwen2.5-VL model in fp8 precision
  --output_format {jsonl,text}
                        Output format: 'jsonl' for JSONL file or 'text' for
                        individual text files (default: jsonl)

DONE
